# 0809 
- 테스트 CI 결과
	백엔드 282 passed / 2 skipped, Python 3.11 - 프론트 474 passed (수정 전 CI에서 8개만 실행) - CI 소요 1분 56초 - 발견: package.json glob이 Linux sh에서 재귀 확장 안 됨 - 발견: src/ 아래 factoryMapLayout.test.js 25개가 실행된 적 없었음
## 1. 인프라

### 1) 8/9(일) ~ 8/22(토) 일정

**스키마 동결일을 8/15로 못 박는 게 이 일정의 핵심입니다.** 그 이후 변경은 컬럼 추가 같은 가산적 변경만 허용하세요.

|날짜|A · 인프라|B · 기능|
|---|---|---|
|**8/9 (일)**|**CI 워크플로우 작성 + 512MB에 배포 연습**|`requirements-dev.txt`, torch 없이 pytest 확인|
|**8/10 (월)**|Dockerfile + docker-compose|DB 리셋 스크립트, 환경변수화|
|**8/11 (화)**|**로컬 전체 구동 검증** ⚠️판단시점|재고관리 개발|
|**8/12 (수)**|Lightsail 4GB 생성, 첫 배포|재고관리 개발|
|**8/13 (목)**|배포 문제 해결 (버퍼)|OpenAI Provider 전환|
|**8/14 (금)**|health check, CD 워크플로우|재고관리 마무리|
|**8/15 (토)**|YOLO fixture + Release 업로드|**🔒 스키마 동결**|
|**8/16 (일)**|스키마 반영 배포, 스모크 테스트|최종 데이터 적재|
|**8/17 (월)**|YOLO 배치 실측, 사양 조정|전 기능 점검|
|**8/18 (화)**|성능 튜닝|데모 시나리오 확정|
|**8/19 (수)**|**전체 리허설**|백업 영상 녹화|
|**8/20 (목)**|**🔒 코드 동결**|발표자료|
|**8/21 (금)**|예비일|리허설 2회차|
|**8/22 (토)**|발표||

중단 기준도 갱신합니다.

- 8/11까지 로컬 docker compose 전체 구동 실패 → AWS 포기, 로컬 데모
- 8/13까지 OpenAI 전환 실패 → 챗봇 데모 제외
- 8/15까지 재고관리 미완 → **미완 상태로 동결**하고 나머지 안정화에 집중
- 8/17까지 서버 YOLO 추론 실패 → 8GB 증설, 그래도 안 되면 사전 계산 결과로 데모






## 0809,0810 테스트 일정
### 3. 오늘·내일 클라우드/CI 테스트 — 가능합니다

**둘 다 오늘 안에 끝낼 수 있습니다.** 그리고 이걸 오늘 하시는 걸 강력히 권합니다. 스키마와 무관하고, 막히는 지점을 일찍 발견할수록 좋습니다.

핵심은 **기존 512MB 연습 인스턴스를 쓰는 것**입니다. 10월 중순까지 무료라 비용이 0이고, 실제 앱은 못 올려도 **배포 파이프라인의 동작 원리는 100% 검증됩니다.** SSH 접속, Docker 설치, GitHub Actions에서 원격 배포 — 이 세 가지가 되는지만 확인하면 8/12에 4GB 인스턴스로 옮길 때 그냥 IP만 바꾸면 됩니다.

#### 8/9 (오늘) 시간 계획

**오전 · CI 구축 (2시간)** — 여기가 가장 확실하게 성공합니다

1. `backend/requirements-dev.txt` 생성, `pytest` 명시 (15분)
2. 깨끗한 venv에서 torch 빼고 설치 → `pytest` 수집되는지 확인 (30분)
3. `.github/workflows/ci.yml` 작성 — backend-test, frontend-build 두 잡 (1시간)
4. feature 브랜치에 push해서 초록불 확인 (15분)

**오후 · 배포 파이프라인 연습 (3시간)** — 512MB 인스턴스 사용

1. 512MB 인스턴스에 SSH 접속, Docker + Compose 설치 (30분)
2. `/health` 하나만 있는 최소 FastAPI를 손으로 배포 → 외부 브라우저에서 응답 확인 (1시간)
3. GitHub Actions에 SSH 키를 Secret으로 등록 (30분)
4. `workflow_dispatch` 방식 배포 워크플로우 작성 → 버튼 눌러서 원격 배포 성공 확인 (1시간)

**오늘의 완료 조건: 버튼 하나 눌러서 GitHub Actions가 서버에 배포하고, 브라우저에서 `/health` 응답이 보이는 상태.**

이게 되면 나머지는 "이 파이프라인에 진짜 앱을 얹는 것"뿐입니다. 심리적으로도 큰 차이가 있습니다.

#### 8/10 (내일)

실제 프로젝트의 Dockerfile과 docker-compose를 작성합니다. **베이스 이미지에 torch가 포함된 것을 쓰세요** — `ultralytics` 공식 CPU 이미지나 `pytorch/pytorch` 위에 앱만 얹으면 빌드가 수십 초로 끝나고, 나중에 4GB 서버에서 직접 빌드해도 메모리 부족이 안 납니다.

주의: **오늘 만든 최소 배포와 내일 만드는 실제 이미지는 별개**입니다. 오늘 건 파이프라인 검증용이니 버려도 됩니다.