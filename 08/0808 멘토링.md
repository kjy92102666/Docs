# 🏭 버퍼 슬롯 물류 상태 및 판정 로직 안내

현재 색상은 작업자 판정 여부가 아니라 **버퍼 슬롯의 물류 상태**를 나타냅니다.

---

## 📊 버퍼 슬롯 상태별 표시 색상

| 시점 | 슬롯 상태 | 표시 색상 |
| :--- | :--- | :--- |
| 비어 있음 | `EMPTY` | 진한 남색 |
| 검사 라인에서 버퍼로 이동 중 | `RESERVED` | 노란색 |
| 버퍼에 적재 완료 | `OCCUPIED` | 청록·파란색 |
| 최종 적재공간 이동 시작 | `EMPTY`로 해제 | 진한 남색 |


---

## 🔄 공정 상태 처리 흐름

1. **검사 완료**
2. **버퍼 이동 시작**: 노란색 (`RESERVED`)
3. **버퍼 도착**: 파란색 (`OCCUPIED`)
4. **작업자 판정 대기**: 계속 파란색
5. **작업자 판정 완료**: 계속 파란색
6. **최종 적재 이동 시작**: 슬롯 비움

---

## 🔍 관련 근거 코드 및 위치

- 슬롯 색상은 `EMPTY`, `RESERVED`, `OCCUPIED`만 구분합니다. `finalQualityVerdict`는 색상 계산에 사용하지 않습니다.
  - `frontend/src/features/process-status/components/FactoryMap.jsx:221`
- `RESERVED`는 노란색, `OCCUPIED`는 청록·파란색입니다.
  - `frontend/src/features/process-status/components/AgvLineControl.css:146`
- 버퍼 이동 시작 시 슬롯을 `RESERVED`로 변경합니다.
  - `backend/app/features/process_status/repository.py:31`
- 버퍼 도착 즉시, 작업자 판정 여부와 무관하게 `OCCUPIED`로 변경합니다.
  - `backend/app/features/process_status/repository.py:113`

---

## ⚖️ 작업자 판정 상태 제어 (별도 관리)

판정 자체는 물류 색상과 별도로 관리됩니다.

* 미판정 검토가 하나라도 있으면 최종 판정은 `null`
* 확정 불량이 하나라도 있으면 `DEFECT`
* 모두 오탐이거나 검토 대상이 없으면 `NORMAL`






# 포트폴리오 작성요령
- 프로젝트
- 기술파트 (DB,). 각자 맡은 파트에는 본인이 발표하기..

*목차 순서대로 자료를 작성해야한다.(보는사람의 스트레스 줄이기.)
1. 색상은 되도록이면 흰색,
2. pdf로 만들어서 뽑을 수 있게(메모가능하도록)
3. 두서없이 길게 작성x 키워드 위주로 짧게.(설명을 덧붙이더라도)
