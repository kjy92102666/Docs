
- [ ] 산출물(erd,wbs,테이블정의서..) 체크해서 최신화
- [ ] 공정상태 (AGV/LINE 상태정보), 재고관리 작업
- [ ] 일단 aws에 DB 올리기!! (추후에 프론트,백 다 올리는걸로.)
- 다음주 목요일까지 PCB 불량검사 (AI 검출 현황)공정상태 (AGV/LINE 상태정보) 마무리.
- 추가로 
- [ ] 추후 프론트 리터칭.

공정상태
1) 배치등록&검사버튼클릭 전 단계에서 취소가 가능한데 회수하는 모션 넣을지?
	>> 라인에서 agv를 호출하는 방식으로 진행할 예정이라 
2) 라인 생산중일때 계획등록,배치등록 가능하게할지?
	>> ???

3) 검사 완료시 불량확정, 오탐처리는 작업하고나서 결정해보기로...




# Worktree 전환 및 PCB 재고 관리 연동 안내

MySQL 및 PCB 데이터셋과의 연결 상태는 정상입니다. 다만, 현재 실행 중인 서버가 새 Worktree가 아닌 기존 저장소 경로를 바라보고 있어 신규 API 호출 시 404 에러가 발생하거나 재고 메뉴가 Placeholder로 표시되고 있습니다.

---

## 1. 현재 상태 확인 요약

- **프론트엔드**: `C:\Users\OWNER\Desktop\pcb-defects-mes\frontend` (재고 메뉴가 여전히 Placeholder)
- **백엔드**: 기존 저장소 코드 실행 중 (`/api/inventory/pcb-wip` 호출 시 404 발생)
- **데이터베이스/데이터셋**: MySQL 3306 포트 연결 정상, PCB 로컬 데이터셋 존재
- **데이터 예외 사항**: `PLAN-20260731-0009`는 이미 `INSPECTED` 상태이므로 이번 검사 전 재고 화면 대상에서 제외되는 것이 정상입니다.
- **현재 예상 표시 데이터**:
  - `PLAN-...0008`: 검사 대기 (PCB 8개)
  - `PLAN-...0004`: 검사 준비 확인 필요

---

## 2. 로컬 실행 순서 (Worktree 적용)

기존 백엔드 및 프론트엔드 터미널에서 `Ctrl + C`를 입력하여 실행 중인 프로세스를 먼저 종료한 후 진행해 주세요.

### Step 1. Worktree 환경설정 파일 복사
새 PowerShell 창을 열고 아래 명령어를 실행하여 `.env` 환경변수 파일을 Worktree 경로로 복사합니다.

```powershell
cd C:\Users\OWNER\Desktop\pcb-defects-mes\.worktrees\pcb-wip-inventory

Copy-Item `
  C:\Users\OWNER\Desktop\pcb-defects-mes\backend\.env `
  .\backend\.env