- [ ] 주간 보고 내용 정리 (멘토링 대화내용 정리)
- [ ] 팀프로젝트(AI_MES_) 소스작업
- [ ] 면접 준비




---
tags:
  - project/agv
  - documentation/backend
  - status/in-progress
date: 2026-08-04
---

# AGV 및 생산계획 Batch 제어 로직 현황 분석

> [!info] 
> 본 문서는 현재 소스 코드 기준의 **생산계획 상태**, **Batch DB 상태**, **AGV 시뮬레이션 상태** 제어 흐름과 예외 처리 및 미연동 항목을 정리한 노트입니다.

---

## 1. 검사 등록부터 검사 시작 전까지

### 1.1 단계별 상태 및 진입 조건

| 단계 | 생산계획 상태 | Batch 상태 | 진입 조건 | 결과 |
| :--- | :--- | :--- | :--- | :--- |
| **검사계획 등록 전** | `없음` | `없음` | 날짜·수량·모델·라인 입력 | 등록 요청 가능 |
| **검사계획 등록 완료** | `PLANNED` | `없음` | 수량 1~500, 오늘 이후 날짜, 존재하는 모델·라인 | 생산계획 생성 |
| **Batch 등록 판정** | `PLANNED` | `생성 전` | 계획 상태, 데이터셋, 라인 점유 상태 검사 | 생성 또는 거부 |
| **빈 라인 Batch 등록** | `BATCH_CREATED` | `LINE_ASSIGNED` | 현재 Batch 없음, Waiting Batch 없음 | 검사 준비 확인 가능 |
| **같은 모델 대기 등록** | `BATCH_CREATED` | `CREATED` | 현재 라인 사용 중, 같은 모델, Waiting 슬롯 비어 있음 | Waiting Batch로 대기 |
| **검사 준비 완료** | `BATCH_CREATED` | `LINE_ASSIGNED` | PCB·이미지·파일 수량 일치 | `inspection_ready = true` |
| **검사 시작 직전** | `BATCH_CREATED` | `LINE_ASSIGNED` | `inspection_ready = true` | 검사 시작 버튼 활성화 |

---

### 1.2 검사계획 등록 (Plan)

* **API**: `POST /api/plans`
* **검증 조건**:
  * `planned_quantity`: 1 ~ 500
  * `plan_date`: 오늘 또는 미래 날짜
  * `model_id`: DB 존재 여부 확인
  * `target_line_id`: DB 존재 여부 확인
* **입력 방식**: `CALENDAR_CLICK` 또는 `EXCEL_BULK`
* **성공 시**: `production_plan.status = PLANNED`
> [!note] 이 단계에서는 Batch 및 PCB가 아직 생성되지 않습니다.

---

### 1.3 Batch 등록 및 라인 판정

* **API**: `POST /api/plans/{plan_id}/batch`
* **기본 조건**:
  1. 생산계획 존재
  2. 생산계획 상태 = `PLANNED`
  3. 희망 라인 지정 완료
  4. 해당 모델의 원본 정상/불량 PCB 데이터 준비 완료

#### 라인 판정 결과별 처리

1. **빈 라인인 경우**
   * `production_plan.status = BATCH_CREATED`
   * `production_batch.status = LINE_ASSIGNED`
   * **의미**: 해당 라인이 Batch를 수용함. AGV 1대가 Batch 전체 운송 및 검사 시작 후보 지정.

2. **같은 모델 검사 중 & Waiting 슬롯 비어있음**
   * `production_plan.status = BATCH_CREATED`
   * `production_batch.status = CREATED`
   * **의미**: Batch 생성 완료되었으나 라인 미배정. Waiting AGV 1대가 적재 대기. 이미지가 준비되어도 검사 시작 불가.

3. **다른 모델 검사 중**
   * **응답**: `HTTP 409 Conflict` (`code: LINE_MODEL_CONFLICT`)
   * **결과**: Batch 및 PCB 생성 불가 (생산계획 `PLANNED` 유지).

4. **Waiting Batch가 이미 존재함**
   * **응답**: `HTTP 409 Conflict` (`code: LINE_WAITING_SLOT_FULL`)
   * **결과**: 두 번째 Waiting Batch 생성 불가 (생산계획 `PLANNED` 유지).

> [!warning] 라인 점유 판단 기준
> 라인 점유 판단 시 다음 Batch 상태를 모두 포함합니다:
> `CREATED`, `LINE_ASSIGNED`, `INSPECTING`, `INSPECTED`  
> *(즉, `INSPECTED` 상태도 회수 완료 전까지는 라인을 점유한 것으로 판단합니다.)*

---

### 1.4 검사 준비 상태 계산 (`inspection_ready`)

`inspection_ready`는 DB 컬럼이 아닌 **Batch 목록 조회 시 동적으로 계산되는 필드**입니다.

#### 필수 충족 조건 (ALL TRUE)
- [x] Batch 상태 == `LINE_ASSIGNED`
- [x] PCB 생성 개수 == `batch_size`
- [x] 이미지가 연결된 PCB 개수 == `batch_size`
- [x] 연결된 모든 이미지 파일이 디스크에 실제 존재

```text
[LINE_ASSIGNED] + [PCB 수량 일치] + [이미지 연결 완료] + [파일 존재]
 └──> inspection_ready = true (검사 시작 가능)
```

---



# 주간 보고 내용 정리

### 1. 인프라 및 무료 클라우드 활용
- **클라우드 플랫폼:** 클라우드플레어(Cloudflare) 및 오라클 클라우드(Oracle Cloud) 무료 티어 활용
- **도커(Docker) 도입 여부:** 도커 사용은 필수가 아님. 도입하지 않거나 도입할 경우 이에 대한 명확한 이유와 명분이 필요함

### 2. UI 검토 및 개발 안정화 일정
- **UI 피드백 반영:** UI/UX 검토 후 전달받은 피드백 사항 개선 추진
- **개발 안정화 목표:** 발표 이틀 전까지 지속적인 테스트 및 시스템 안정화 작업 진행하게될것.

### 3. 모델 테스트 결과 (우리팀X, 참고만 하기)
- **전체 모델 분석 속도:** 내부 테스트 진행 결과, 이전 모델들의 처리 속도가 지연되는 현상 확인 및 대응 필요
- **RAG 성능 향상 방안:** RAG(검색증강생성)는 입력되는 기본 문서의 품질이 우수할수록 출력 결과의 품질도 함께 향상됨 (분석 기준 체계화 필요)

### 4. 일의 효율 및 협조 요청 (야근 관련)
- **야근 사전 보고 프로세스:** 야근이 필요한 상황 발생 시 강사님 및 협회에 사전 요청 필수
- **보고 시한:** 최소 하루 전까지 사전 보고 완료 체계 준수

### 5. 형상 관리 및 프로젝트 진행
- **소스 코드 최신화:** 팀원 간 작업 내용 유실 방지 및 안정적인 배포를 위해 최신 소스 코드 유지 관리. 
- 팀별 멘토링 및 작업내용 기록하기.


### 6. 발표자료 관련
- 발표 자료는 젠스파크를 통해 작업할 예정이니 만드는부분에 부담 가지지않기. (단, 발표할 내용은 정리해서 가지고 있기!!) 

추가로 오늘부터 각 팀별로 소스 확인 후 피드백 주어진다고 하셨어요. 





### 

# 검사화면 테이블 수정제안(기운님 소스 적용 후 꼭 확인해보기)

`inspection_result` 하나로 구현해도 괜찮습니다. 별도 테이블은 필수가 아닙니다.

다만 다음 전제가 필요합니다.

> `inspection_result` 1행은 PCB 전체 결과가 아니라 하나의 검출 객체 또는 검출되지 않은 PCB의 결과를 나타낸다.

그리고 저장·조회·진행률을 모두 이 기준에 맞춰 변경해야 합니다. 검사 담당자의 현재 요구사항과 “재검사 없음” 조건이라면 단일 테이블 방식으로 진행해도 충분합니다.

- 단일 `inspection_result` 테이블로도 향후 집계·통계 구현은 가능합니다.
- 다만 모든 개발자가 “행 수는 PCB 수가 아니다”라는 규칙을 지켜야 합니다.
- 단순 통계 메뉴가 중심이면 단일 테이블로 진행해도 됩니다.
- PCB 단위 분석과 박스 단위 분석이 많아질 예정이라면 `inspection_result + inspection_detection` 분리가 장기적으로 더 안전합니다.

현재 일정과 재검사가 없다는 조건을 고려하면 단일 테이블로 진행할 수 있지만, 통계 설계 문서에 `PCB 단위`와 `검출 객체 단위`를 반드시 구분해서 명시하는 것을 권장합니다.


























