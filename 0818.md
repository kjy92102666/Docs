지금 17:04 기준으로 보면, 오늘 원래 잡았던 스케줄에서 **1~3번은 사실상 끝났고, 4번이 진행 중, 5번과 이미지 버그가 남아 있어.**

## 전체 상태를 다시 정리하면 이래.

1. **Dashboard 우측 카드 CSS 확인** → ✅ 완료  
    `PROCESS & AGV / 공정 상태`, `INVENTORY / 재고관리` 스타일 실제 브라우저까지 확인했고 관련 커밋도 이미 `dev/main`에 반영된 상태.
2. **브랜치 정리 + main 배포 + AWS 확인** → ✅ 완료  
    AWS 서버의 untracked 수동 스크립트 충돌도 정리했고, `main` 기준 배포 성공, 컨테이너와 `/health/ready`까지 정상 확인.
3. **CI MariaDB 서비스 + analysis export CI skip 제거** → ✅ 완료  
    `mariadb:lts` 기반 CI DB 구성, `schema.sql` 적용, export 테스트 skip 제거했고 최종적으로 `399 passed, 2 skipped`. PR #50도 현재 `dev`에 병합 완료돼서 `dev/origin/dev = ea3cf61`.
4. **운영 DB Migration 안전 배포 체계** → 🔄 현재 진행 중  
    여기서 4-1 AWS 운영 DB read-only audit는 이미 ✅ 완료했고, 지금은 **4-2 `schema_migrations` / forward-only migration 설계**를 Codex가 진행 중이야.
    
    설계 결과가 오면 아직 남은 건:
    
    - 설계 검토
    - `schema_migrations` 실제 구현
    - `deploy_migrations/` 구조 구현
    - migration runner 구현
    - buffer slot 1~16 bootstrap 보완
    - CI에서 migration/bootstrap 검증
    - DDL 적용 후 history 미기록 엣지케이스 방어
    - pre-deploy DB backup 정책 확정
    - `deploy.yml`에 안전 migration 흐름 연결
    - 실제 AWS 배포 테스트
5. **GitHub Default Branch `dev → main` 복구** → ⏳ 미진행  
    이건 마지막에 하면 돼. Default Branch만 `main`으로 되돌리고, 팀 작업 흐름은 계속 `feature → dev → main`으로 유지하면 돼.

그리고 오늘 일정에 원래 없었지만 중간에 발견된 **PCB 이미지 표시 오류**가 별도 작업으로 남아 있어. AWS에서는 `/api/pcb/PCB-00801/image`가 실제 JPEG로 `200 OK`라서 서버 파일/백엔드 API는 정상이고, 프론트 화면에서는 placeholder가 뜨는 상태야. 이건 migration 작업 하나 끝낸 뒤 Claude에게 **단일 작업으로** 맡기기로 했지.

그래서 현실적인 남은 순서는 이렇게 잡으면 가장 깔끔해.

[현재]

4-2 schema_migrations 설계

        ↓

설계 결과 검토/승인

        ↓

4-3 migration 체계 실제 구현

        ↓

CI 검증

        ↓

PCB 이미지 표시 오류 수정

        ↓

필요하면 이미지 수정 먼저 dev→main→AWS 배포

        ↓

4-4 deploy.yml migration + pre-deploy backup 연결

        ↓

전체 dev→main

        ↓

AWS 최종 배포/검증

        ↓

5. GitHub Default Branch → main

        ↓

오늘 작업 종료

즉 지금 기준으로 **핵심 미완료 작업은 4번 migration 체계 구현, 이미지 오류 수정, 최종 AWS 배포, Default Branch 복구** 네 덩어리라고 보면 돼.

특히 오늘 발표 전 안정성을 우선하면, `schema_migrations` 설계가 너무 커지거나 실제 구현이 예상보다 오래 걸릴 경우에는 **이미지 오류 수정과 현재 기능 안정화까지 먼저 끝내고**








## 8/18 전체 작업 현황

| 구분             | 작업                                   |        상태 | 현재 결과                                                |
| -------------- | ------------------------------------ | --------: | ---------------------------------------------------- |
| 1              | Dashboard 우측 카드 CSS 확인               |      ✅ 완료 | 공정상태/재고관리 카드 스타일 실제 브라우저 확인                          |
| 2              | 브랜치 정리 + main AWS 배포                 |      ✅ 완료 | AWS 최신 main 배포 및 health 정상                           |
| 3              | CI MariaDB + analysis export skip 제거 |      ✅ 완료 | PR #50 → dev 병합, CI 정상                               |
| 4-1            | 기존 migration 전수조사                    |      ✅ 완료 | legacy 11개 분류 완료                                     |
| 4-2            | AWS 운영 DB read-only audit            |      ✅ 완료 | 최신 schema와 동등, 미적용 forward migration 0개              |
| 4-3            | Migration 안전 배포 체계 설계                |      ✅ 완료 | `schema_migrations`, runner, bootstrap, backup 구조 확정 |
| 4-4 Phase 1    | Migration runner 구현                  |      ✅ 완료 | commit `8728256`, 테스트 통과                             |
| 4-4 Phase 1 인계 | 원격 브랜치 push                          | ⏳ 지금 할 작업 | 집 노트북 작업 인계용                                         |
| 4-5 Phase 2    | Buffer bootstrap + CI 보완             |      ⏳ 남음 | 집에서 이어서 진행                                           |
| 4-6 Phase 3    | pre-deploy backup + deploy.yml       |      ⏳ 남음 | migration 자동배포 연결                                    |
| 별도             | PCB 이미지 표시 오류                        |      ⏳ 남음 | migration 작업 후 단일 작업으로 처리                            |
| 4-7            | dev → main + AWS 최종 배포               |      ⏳ 남음 | 전체 검증 후                                              |
| 5              | GitHub Default Branch → main         |     ⏳ 마지막 | 최종 정리                                                |
