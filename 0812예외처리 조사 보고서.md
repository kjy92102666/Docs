# 🔍 백엔드 및 프론트엔드 예외 처리 및 안정성 조사 보고서

백엔드 각 Feature 모듈(`batch`, `inspection`, `process_status`, `review`, `inventory`, `chatbot`, `yolo adapter`, `dataset`)과 프론트엔드 핵심 부분(API 클라이언트, WebSocket 클라이언트, 엔트리포인트)을 직접 읽고 확인한 결과입니다.

---

## A. 이미 잘 처리되어 있는 부분

* **구조화된 에러 컨벤션**
  * `common/errors.py`의 `structured_error(status, code, message, guidance)`를 대부분의 서비스 계층이 일관되게 사용합니다.
  * 프론트 `apiError.js`가 이 shape을 그대로 소비해서 화면마다 "무엇이 문제고 어떻게 해야 하는지"를 통일된 톤으로 보여줍니다.

* **YOLO 어댑터 (`yolo/adapter.py`, `yolo/exceptions.py`)**
  * 모델 로드 실패 / 추론 실패 / 결과 파싱 실패 / 설정값 오류를 각각 별도 예외 클래스로 구분합니다.
  * `inspection/service.py`가 이를 받아 `502 YOLO_INFERENCE_FAILED`로 깔끔하게 변환합니다.

* **Dataset 접근 (`dataset.py`)**
  * Path traversal 방지(`..`, 절대경로, 백슬래시 차단)가 적용되어 있습니다.
  * 마운트된 드라이브라 디렉토리 전체 스캔 대신 루트 존재만 확인하며(주석에 이유 명시), 파일 없음 / 키 오류 / 루트 없음을 구분된 코드로 던집니다.
  * 호출부(`batch/service.py`, `inspection/service.py`, `inspection/router.py`)가 케이스별로 정확히 분기 처리합니다.

* **Ollama 챗봇 연동**
  * Connect 실패 / Timeout / HTTP Status / JSON 파싱 실패를 각각 잡아서 사용자에게 "지금 사용 불가" 같은 안내로 변환합니다.
  * 대시보드/검사 컨텍스트 로딩 실패도 챗봇 전체를 죽이지 않고 개별 블록만 스킵합니다.

* **동시성 제어 (매우 촘촘함)**
  * `process_status/service.py`: 라인 / 버퍼슬롯 / 전송(transfer)마다 `FOR UPDATE` 락 + Idempotency 체크(같은 `transfer_id`로 재요청해도 에러 없이 같은 응답). 대기 배치가 2개 이상이면 `LINE_WAITING_DATA_CONFLICT`로 명시적 차단.
  * `inventory/service.py`: `handoff_batch` — 두 배치가 같은 부품을 동시에 인계 요청할 때 재고가 음수로 내려가는 걸 막으려고 부품 행을 잠근다는 코드 주석 보유 (113~114줄).
  * `inventory/calc.py`: `classify_verdict` — `required_qty <= 0` 0-division 가드가 존재합니다.

* **YOLO 배치 실행기 (`inspection/batch_runner.py`)**
  * PCB 1개 추론 실패 시 배치 전체를 죽이지 않고 `FAILED` 이벤트로 정지하며, 프론트의 `STALE`/재시작 로직과 맞물려 재개 가능하게 설계되어 있습니다.
  * Lifecycle hook(공정상태 갱신)은 "best-effort"라고 주석으로 명시되어 있고, 실패해도 검사완료 트랜잭션 자체에는 영향이 없도록 분리되어 있습니다.

* **WebSocket**
  * 서버/클라이언트 양쪽 Disconnect 처리가 되어 있습니다.
  * 클라이언트가 끊기면 자동 재연결 후 `resyncSelectedBatch()`로 상태를 복구합니다.
  * 잘못된 JSON 메시지는 무시하고 로그만 남깁니다.

---

## B. 예외 처리가 부족하거나 필요한 부분

### 1. (가장 중요) 전역 500 핸들러가 없음
* `backend/app/main.py`에 등록된 핸들러는 `RequestValidationError` 하나뿐입니다 (21번째 줄).
* 일반 `Exception`에 대한 핸들러가 없어서, `structured_error` 컨벤션을 안 쓴(=캐치 안 한) 에러는 전부 FastAPI 기본 500(코드/메시지/가이던스 없는 raw 응답)으로 나갑니다.
* 실제로 서비스 계층을 전수 조사해보니 DB 관련 예외를 구조화된 에러로 감싸는 곳이 딱 한 곳뿐입니다.
  * `inspection/service.py`의 `SinglePcbYoloInspectionService.infer_and_save` (276~284줄, `except HTTPException: raise` / `except Exception: raise structured_error(500, "INSPECTION_RESULT_SAVE_FAILED", ...)`)
  * 나머지 `batch/service.py`, `process_status/service.py`, `review/service.py`, `inventory/service.py`는 DB 에러가 나면 다 raw로 새어나갑니다.
* 프론트(`apiError.js`)가 non-JSON 응답을 방어적으로 처리해서 앱이 죽진 않지만, 사용자는 "요청에 실패했습니다 (HTTP 500)"라는 의미 없는 메시지만 보게 됩니다. (다른 곳들의 친절한 code/message/guidance UX가 여기서만 깨짐)

### 2. DB 락 대기 / 데드락에 대한 처리 없음
* `core/database.py`의 `transaction()`은 예외 시 Rollback만 하고 그대로 재-raise합니다.
* 위에서 본 것처럼 동시성을 상당히 의식하고 `FOR UPDATE` 락을 적극적으로 쓰는데, 정작 MySQL이 실제로 던질 수 있는 `1205 Lock wait timeout` / `1213 Deadlock found`에 대한 재시도나 친절한 메시지가 없습니다. (1번 문제와 직결됨)

### 3. `async def` 라우트 안에서 동기 DB 호출이 이벤트 루프를 블로킹
* `process_status/router.py`, `batch/router.py` 등 다수가 `async def` 핸들러인데, 내부에서 `pymysql` 기반 동기 함수(`run_lifecycle_command`, `service.create_batch` 등)를 스레드풀 오프로드 없이 직접 호출합니다.
* DB가 느려지거나 락 대기가 걸리면 그 시간만큼 이벤트 루프 전체가 멈춰서, 동시에 열려 있는 `/ws/inspections`, `/ws/process-status` 소켓 메시지 처리와 다른 API 요청까지 같이 지연됩니다.
* 엄밀히는 "예외 처리"보다 "예외적 상황(DB 지연)에서의 안정성" 문제입니다.

### 4. 프론트 React 전역 `ErrorBoundary` 없음
* `frontend/src/main.jsx`가 `<App/>`을 바로 렌더링합니다.
* 어디든 렌더링 중 처리 안 된 예외(예상 못한 API 응답 형태로 `undefined.xxx` 접근 등) 하나만 터져도 화면 전체가 흰 화면이 됩니다.
* 지금까지 본 컴포넌트들이 Optional Chaining을 꽤 꼼꼼히 쓰고 있어 실제 발생 확률은 낮아 보이지만, 안전망 자체가 없다는 건 마무리 단계에서 짚을 만합니다.

### 5. Dataset 경로가 Windows 로컬 드라이브(Google Drive 마운트) 하드코딩 — Docker 작업과 직결
* `.env`의 `PCB_DATASET_PATH=G:/.shortcut-targets-by-id/.../PCB_DATASET_DATA`.
* `dataset.py` 자체는 이 상황을 `DatasetError("DATASET_ROOT_NOT_FOUND")`로 깔끔하게 처리해서 "예외 처리 안 됨"은 아니지만, 컨테이너 안에는 `G:\` 드라이브가 없으므로 바인드 마운트나 경로 치환 없이 그대로 도커라이즈하면 배치의 `inspection_ready`가 전부 조용히 `false`로 떨어집니다.
* 에러가 안 나서 오히려 원인 파악이 더 어려운 케이스라, 지금 Codex가 진행 중인 `docker` 브랜치에서 반드시 짚어야 할 지점입니다.

### 6. (경미) WebSocket 재연결에 Backoff cap 없음
* `inspectionWebSocket.js`가 고정 1.5초 간격으로 무한 재시도합니다.
* 서버가 오래 다운되면 계속 짧은 간격으로 재연결을 시도합니다. (심각하진 않지만 exponential backoff 정도는 고려할 만합니다. 다른 WebSocket 클라이언트인 `processStatusWebSocket.js`, `inspectionProgressWebSocket.js`도 같은 패턴인지는 확인 필요.)

---

## 💡 추천 방향 및 결론

우선순위를 매기면 **1번(전역 500 핸들러)**이 가장 파급력이 크고 고치기도 제일 쉽습니다. 

`main.py`에 `Exception` 핸들러 하나를 추가해서 최소한 로깅 + 공통 `INTERNAL_ERROR` 구조화 응답으로 통일하는 것부터 시작하는 것을 추천드립니다. 원하시면 이 중 어느 것부터 손댈지 정해서 진행해 드리겠습니다.