- [ ] DB연동시킨 후 ci구현
- [ ] 팀프로젝트에 적용가능한지 체크(강사님, 멘토님 물어보기)









```




# FastAPI 프로젝트 구조 리팩토링 제안

## 개요

현재 팀원이 검사 기능을 **Route → Service → Repository** 구조로 분리한 방향은 적절하다.

다만 다음과 같이 `routes`, `services`, `repositories`를 `backend/` 바로 아래 두는 구조는 추천하지 않는다.

```text
backend/
├── app/
│   └── main.py
├── routes/
├── services/
└── repositories/
```

FastAPI 애플리케이션 코드가 여러 최상위 패키지로 분산되어 import 관리가 어려워질 수 있다.

---

# 권장 프로젝트 구조

```text
backend/
├── app/
│   ├── main.py
│   ├── database.py
│   ├── dataset.py
│   ├── schemas/
│   │   └── inspection.py
│   ├── routes/
│   │   └── inspection.py
│   ├── services/
│   │   └── inspection_service.py
│   └── repositories/
│       └── inspection_repository.py
├── db/
├── scripts/
└── tests/
```

---

# 계층별 역할

| 계층 | 역할 |
|------|------|
| **main.py** | FastAPI 앱 생성, Middleware 등록, Router 등록 |
| **schemas** | Pydantic 요청(Request) / 응답(Response) 모델 |
| **routes** | HTTP 요청 처리, 상태코드 반환, Service 호출 |
| **services** | 비즈니스 로직(업무 규칙) |
| **repositories** | SQL 실행 및 DB 조회/저장 |
| **database.py** | DB 연결 및 트랜잭션 관리 |
| **dataset.py** | Google Drive 이미지 경로 관리 |

---

# 호출 흐름

```text
HTTP Request
      │
      ▼
Route
      │
      ▼
Service
      │
      ▼
Repository
      │
      ▼
Database
      │
      ▼
MySQL
```

### 의존성 규칙

다음과 같은 역방향 참조는 금지한다.

```text
Repository → Service ❌
Service → Route ❌
Database → Repository ❌
```

---

# 검사 기능 예시

## Route (`routes/inspection.py`)

담당 역할

- GET /api/batches/{id}/inspections
- POST /api/batches/{id}/inspect
- HTTP 요청 처리
- 응답 반환
- HTTPException 처리

---

## Service (`services/inspection_service.py`)

담당 역할

- 배치 검사 가능 여부 판단
- 이미지 준비 여부 확인
- 검사 시작 및 완료 상태 변경
- Vision 결과 저장 규칙 결정

---

## Repository (`repositories/inspection_repository.py`)

담당 역할

- Batch 조회 SQL
- PCB 조회 SQL
- Inspection Result 저장
- DB 트랜잭션 처리

---

# Schema 관리

Pydantic 모델은 Route 내부에 작성하지 않는다.

```text
schemas/
└── inspection.py
```

에서 Request/Response 모델을 관리한다.

> AGENTS.md에서도 FastAPI 라우터 변경 시 Pydantic 모델을 먼저 정의하는 것을 권장하고 있다.

---

# Frontend 구조

## 권장 구조

```text
frontend/src/
├── api.js
├── pages/
│   ├── PcbInspection.jsx
│   └── ProcessStatus.jsx
├── components/
│   ├── inspection/
│   └── process-status/
├── services/
│   └── inspectionService.js
└── domain/
    ├── inspectionWorkflow.js
    ├── agvControlSimulation.js
    └── agvControlMetrics.js
```

---

# Frontend 역할

| 위치 | 역할 |
|------|------|
| **api.js** | Base URL, fetch, JSON, 공통 오류 처리 |
| **inspectionService.js** | 검사 API 호출 |
| **domain** | 비즈니스 로직 |
| **pages** | 화면 상태 및 호출 순서 관리 |
| **components** | UI 표시 전용 |

---

# api.js 사용 원칙

## ❌ 나쁜 예

```text
api.js
└── runInspection()

inspectionService.js
└── runInspection()
```

동일한 기능이 중복됨.

---

## ✅ 좋은 예

```text
api.js
└── request(path, options)

inspectionService.js
├── listBatchInspections()
├── runInspection()
└── decideReview()
```

즉,

- **api.js** → 공통 HTTP 요청만 담당
- **Service** → 실제 API 기능 담당

---

# AGV 화면 구조

현재 AGV는 브라우저 시뮬레이션이므로

Route / Service / Repository 구조가 필요하지 않다.

현재 구조 유지

```text
pages
    ↓
components
    ↓
domain
```

추후 WebSocket을 붙일 경우

```text
services/
└── processStatusService.js
```

를 추가한다.

---

# .ai 폴더 운영

`.ai` 폴더는 사용할 수 있다.

다만 기존 문서와 역할이 겹치지 않도록 최소한으로 유지한다.

```text
.ai/
├── STATUS.md
└── OWNERSHIP.md
```

에이전트 규칙은 계속

```text
AGENTS.md
```

를 단일 기준으로 사용한다.

---

# 팀에서 합의해야 할 사항

## 1. 폴더 위치

다음 중 어느 구조를 사용할지 결정

```text
backend/app/
```

또는

```text
backend/
```

---

## 2. 적용 범위

Route → Service → Repository 구조를

- 검사 기능만 적용할 것인지
- 전체 백엔드 표준으로 사용할 것인지

팀 차원에서 결정한다.

---

# 최종 추천

## Backend

- `backend/app` 내부에
  - routes
  - services
  - repositories
  - schemas
- 검사 기능부터 점진적으로 계층 분리

---

## Frontend

현재 구조 유지

```text
pages
    ↓
components
    ↓
domain
```

실제 API(WebSocket)가 생길 때만 Service 추가

---

## 핵심 결론

- Route → Service → Repository 구조는 유지보수 측면에서 바람직하다.
- FastAPI 관련 코드는 `backend/app` 내부에 모아 관리하는 것이 좋다.
- Pydantic 모델은 `schemas`에서 관리한다.
- AGV 화면은 현재 구조를 유지한다.
- 전체 프로젝트 표준으로 적용할지 여부는 팀 합의를 거쳐 결정한다.