


# 오전 작업.
**다음 우선순위 (8/19)**

- 로고 수정화면 : frontend/src/app/AppLayout.css의 .brand-mark 규칙 (3번째 줄)에서 width/height를 34px → 68px

6. DB Migration 안전 배포 체계 — AWS 실제 배포(`Run workflow`)와 최종 검증 (predeploy backup + migration gate 첫 실전 적용, 컨디션 좋은 시간대에 신중히 진행)
7. 대시보드 로고 이미지 반영 완료
8. Default Branch를 `main`으로 원복 (8/13부터 이월, `datamon78` 계정 필요)
9. 여러 브라우저 동시 접속 시연 리허설 (AGV 애니메이션·공정상태·계획등록 동기화 확인 — 8/16 AGV 동기화 버그 재발 여부 포함)
10. (여유 시간) `FastAPI on_event` deprecation 등 사소한 정리

```
ubuntu@ip-172-26-4-140:~/pcb-defects-mes$ ls -lh ~/backups/predeploy/ total 448K -rw-rw-r-- 1 ubuntu ubuntu 445K Aug 19 01:48 predeploy_20260819_014821_9b1f0f5.sql ubuntu@ip-172-26-4-140:~/pcb-defects-mes$ ls -lh ~/backups/ total 924K -rw-rw-r-- 1 ubuntu ubuntu 0 Aug 17 12:17 backup.log -rw-rw-r-- 1 ubuntu ubuntu 23K Aug 15 07:06 backup_20260815.sql -rw-rw-r-- 1 ubuntu ubuntu 445K Aug 17 12:17 backup_20260817.sql -rw-rw-r-- 1 ubuntu ubuntu 445K Aug 18 03:00 backup_20260818.sql drwxrwxr-x 2 ubuntu ubuntu 4.0K Aug 19 01:48 predeploy ubuntu@ip-172-26-4-140:~/pcb-defects-mes$

```
Phase 3의 마지막 검증까지 전부 정상입니다.

- `predeploy_20260819_014821_9b1f0f5.sql` — 파일명에 배포 시각(01:48:21)과 배포한 커밋 SHA(`9b1f0f5`)가 정확히 찍혀 있고, 445K로 0바이트가 아님 → **predeploy backup이 설계대로 실제 배포 직전에 정상 생성됨**
- 기존 daily backup(`backup_20260815.sql`, `backup_20260817.sql`, `backup_20260818.sql`)과 `predeploy/` 폴더가 완전히 분리되어 있음 → **경로/이름 충돌 없음, 설계대로**

- 이걸로 서버 쪽 검증은 다 끝났습니다:
	1. ✅ 컨테이너 정상 (backend만 새로 교체, mariadb/nginx는 무중단 유지)
	2. ✅ Git 상태 배포 커밋과 일치, clean
	3. ✅ `schema_migrations` 테이블 정상 생성, 0건(정상)
	4. ✅ predeploy backup 정상 생성, 기존 백업과 안 겹침
	5. ✅ Health check 통과

- 두 가지 다 확인됐습니다 — 좌측 상단 **로고**(K-Defects-Hunters) 정상 표시, PCB **이미지**도 회귀 없이 잘 뜹니다. Phase 3 배포는 성공적으로 마무리됐습니다.





# 오후 작업.📋 QA 및 E2E 테스트 실행 전략 가이드라인

> **주요 전략 요약**  
> TC-01부터 TC-25까지 단순 순서대로 실행하기보다 **데이터 흐름과 위험도 기준**으로 묶어서 테스트하는 것이 효율적입니다.  
> 현재 Phase 3 운영 배포는 성공했지만 최종 YOLO 모델은 미배포 상태이므로, **테스트를 3단계로 나누어 진행**합니다.

---

## 1. 지금 바로 할 수 있는 테스트 (YOLO 미의존 항목)

최종 YOLO 모델을 기다릴 필요 없이, 이미 검증되었거나 인프라 관련인 항목부터 먼저 닫습니다.

### A. 이미 검증된 인프라 항목 (즉시 결과 기록 가능)

| TC 항목     | 내용            | 현재 판단 | 대응 및 증적 방안                             |
| :-------- | :------------ | :---: | :------------------------------------- |
| **TC-20** | 동일 오리진 URL 회귀 |   ✅   | 운영 화면 정상 확인 완료. 재확인만 진행                |
| **TC-21** | CI 3개 Job     |   ✅   | GitHub Actions 결과 확인 후 즉시 기록 가능        |
| **TC-22** | 수동 배포·헬스체크    |   ✅   | 오늘 Phase 3 결과 자체가 강한 증적이므로 **재배포 불필요** |
| **TC-23** | DB 외부 격리·최소권한 |  ⚠️   | 별도 확인 필요                               |
|           |               |       |                                        |

> ⚠️ **주의 (TC-22 관련):**  
> 굳이 배포를 한 번 더 할 필요가 없습니다. 오늘 확보한 아래 증적을 그대로 실제 결과로 넣으세요. 괜히 테스트 목적으로 production deploy를 다시 하는 것은 위험합니다.
> * `AWS HEAD = 9b1f0f5`
> * `predeploy backup 정상`
> * `backend 교체 정상`
> * `mariadb healthy`
> * `nginx 정상`
> * `/health 정상`
> * `git clean`

---

## 2. 가장 먼저 실행할 기능 흐름 (단일 생산 Batch 추적)

기능별로 분리된 TC를 **하나의 생산 Batch 흐름**에 맞춰 순차적으로 재배치하여 테스트합니다.
[생산계획] ➔ [배치] ➔ [재고 확인] ➔ [검사 준비] ➔ [검사] ➔ [작업자 판정] ➔ [AGV / 공정상태] ➔ [재고 반영] ➔ [Dashboard] ➔ [분석 / 리포트]

  
실제 테스트 작업 시 순서대로 체크하며 진행하기 쉽도록 가독성을 최적화했습니다.

### ① 생산계획 ➔ Batch
1. **TC-01 생산계획 정상등록**
2. **TC-03 배치 정상생성**
   * 위 두 개를 한 묶음으로 테스트합니다.
   * **테스트용 데이터 명확화:** 예) `발표전_TEST_A` ➔ `Plan` ➔ `Batch` ➔ `PCB`
   * **결과 작성 팁:** 단순히 `"정상"`이라 쓰지 말고, 아래와 같이 실제 ID와 상태값을 명시하세요.
     > `PLAN-20260819-0001 생성`  
     > `status=PLANNED 확인`  
     > `BATCH-xxxx 생성 확인`  
     > `계획 status=BATCH_CREATED 전환 확인`

### ② 재고 확인
3. **TC-14 부품 소요량·부족재고**
   * TC-01, TC-03에서 만든 Batch 데이터를 그대로 연결하여 확인합니다.
   * `TC-01 Plan` ➔ `TC-03 Batch` ➔ `TC-14 자재 요구량 확인`
   * *이 방식은 TC-25 E2E 리허설을 미리 진행하는 것과 동일한 효과를 줍니다.*

---

## 3. YOLO 최종 모델 오기 전/후 분리 기준

### 🟢 최종 모델 도착 전 사전 확인 가능 항목
* 검사 시작 버튼
* 검사 상태 전환
* 검사 API 호출
* UI 상태
* 공정상태
* WebSocket
* 검사 준비조건

### 🟡 최종 완료 판정 보류 항목 (YOLO 반영 후 재테스트 필수)
* **TC-05** 검사/AI
* **TC-06** 작업자 검수
* **TC-07** 최종판정 잠금
* **TC-24** 다중라인 YOLO 검사
* **TC-25** 전체 E2E

> 📝 **테스트 결과 표기 가이드:**  
> 억지로 '통과'를 찍지 말고 **`보류`**로 둔 뒤, 실제 결과에 아래와 같이 명확히 적어둡니다.  
> *"최종 YOLO 모델 미배포로 AI 추론 최종 검증 대기 / UI 및 API 흐름까지만 사전 확인"*

---

## 4. 오늘(8/19) 추천 실제 실행 순서

### 1차 — 기본 정상 흐름 (P0 우선 처리)
1. **TC-01** 생산계획
2. **TC-03** 배치
3. **TC-14** 재고 요구량
4. **TC-08** 공정상태
5. **TC-16** Dashboard
6. **TC-18** 분석/리포트
7. **TC-19** 챗봇
8. **인프라 항목 기록:** **TC-20**, **TC-21**, **TC-22** 증적 첨부 후 결과 기록

---

## 5. 예외 및 경계조건 테스트 (P1 항목)

정상 흐름 확인 후 진행하며, 정상 테스트 데이터를 깨뜨리지 않도록 **별도의 예외 테스트용 데이터 세트**를 사용합니다.

* **추천 실행 대상:**  
  * **TC-02** 입력 검증 / 계획 취소
  * **TC-04** 라인 충돌
  * **TC-07** 판정 잠금
  * **TC-11** 이송 멱등성
  * **TC-13** STALE
  * **TC-15** SMD 인계
  * **TC-17** API 실패 복구
  * **TC-23** DB 보안

* **테스트 데이터 분리 권장:**
  * `TEST_NORMAL`: 발표 및 E2E 정상 흐름용
  * `TEST_EXCEPTION`: 오류, 예외, 경계조건 검증용

---

## 6. 특히 조심해야 할 위험 테스트 3가지

### 🚨 TC-10 Buffer 16슬롯 포화
* 실제 운영 DB에서 16개 슬롯을 전부 `OCCUPIED` 상태로 만들어야 할 수 있습니다.
* 잘못하면 이후 정상 테스트 전체를 막아버립니다.
* **대응:** 자동 생성 전에 데이터 생성 및 복구 방법을 먼저 확인하고, 테스트 종료 후 **`1~16 슬롯 정상상태 복원`**까지 반드시 확인하세요.

### 🚨 TC-12 동시 이송 데드락
* Production DB에서 무작정 동시에 클릭하는 방식은 위험합니다.
* **대응:** 기존 테스트 코드나 Integration Test가 있다면 자동 테스트 증적 및 제한된 실제 검증으로 대체하세요. 발표 전 운영 DB를 일부러 극한 경합 상태로 만드는 것은 우선순위가 낮습니다.

### 🚨 TC-17 API 강제 실패
* 실제 AWS Backend를 직접 죽이는 방식으로 진행하면 안 됩니다.
* **대응:** DevTools, Mock, 잘못된 Endpoint 호출, 기존 테스트 코드 등을 활용해 UI 오류 처리 반응을 확인하세요. 발표 서버 전체를 장에 상태로 만드는 방식은 피해야 합니다.

---

## 7. 최종 합격 시험: TC-25 (발표 핵심 E2E)

모든 개별 TC 통과 및 최종 YOLO 모델 배포가 완료되면, **새로운 깨끗한 Plan**을 하나 생성하여 전체 프로세스를 처음부터 끝까지 수행합니다.

### 🔄 TC-25 최종 리허설 시나리오
1. ① 검사계획 등록
2. ② Batch 생성
3. ③ PCB 연결
4. ④ 라인 투입
5. ⑤ 검사 시작
6. ⑥ YOLO 추론
7. ⑦ 검사 결과
8. ⑧ 작업자 정상/불량/오탐 판정
9. ⑨ 검사 완료
10. ⑩ AGV 이송
11. ⑪ 후검사 Buffer
12. ⑫ 재고 반영
13. ⑬ 공정상태 확인
14. ⑭ Dashboard 확인
15. ⑮ 분석/리포트 확인

> 💡 **중요:** TC-25 성공 데이터 세트는 발표 리허설 및 실제 발표용 데이터로 보존하고 더 이상 건드리지 않습니다.

---

## 8. 체계적인 테스트 증적 관리 방법

모든 스텝을 캡처할 필요 없이 **P0 주요 지점 위주**로 아래와 같이 파일명을 체계적으로 정리합니다.

* **정상 흐름 증적 예시:**
  * `TC01_plan_created.png`
  * `TC03_batch_created.png`
  * `TC05_inspection_result.png`
  * `TC06_worker_review.png`
  * `TC08_process_status.png`
  * `TC14_inventory.png`
  * `TC16_dashboard.png`
  * `TC18_report.xlsx`
  * `TC21_github_actions.png`
  * `TC22_deploy_health.png`
  * `TC25_e2e_final.png`

* **결함 발생 및 수정 증적 예시:**
  * `TC05_FAIL_01.png`
  * `TC05_FIX_01.png`
  * `TC05_RETEST_PASS.png`

---

## 📌 결론 및 오늘(8/19)의 최종 목표

1. **오늘의 목표:** P0 항목 중 YOLO 의존 항목(TC-05, 06, 07, 24, 25)을 제외한 **모든 P0 항목 통과**.
2. **YOLO 배포 후:** **TC-05, TC-06, TC-07, TC-24, TC-25**를 묶어서 최종 검사 흐름 일괄 검증.
3. **최종 완료 조건:** **TC-25 통과 + P0 미해결 치명 결함 0건** 달성 시 신규 기능 변경을 Freeze하고 발표 리허설에 집중합니다.

*(기존 엑셀 계획 양식은 그대로 유지하며, 위의 순서와 가이드라인대로 운용하시면 됩니다.)*