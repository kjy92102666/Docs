
# 📌 8/18 DB 마이그레이션 자동화 및 배포 파이프라인 안전 체계 구축 보고

> **발표용 요약 흐름**  
> 발표에 쓰실 거면 흐름을 정확히 짚는 게 중요하니, 시작점부터 끝까지 이어서 설명드릴게요.

---

## 0. 모든 것의 시작 — 8/16에 실제로 터진 사고

8/16에 팀이 배치·검사 데이터를 채워 넣다가 이런 일이 있었습니다.  
검사가 끝난 배치를 다음 단계(버퍼)로 옮기려는데 **"버퍼가 꽉 찼습니다"**라는 에러가 떴습니다. 그런데 실제로는 버퍼가 텅 비어 있어야 정상인 상황이었어요.

### 🔍 원인을 파고들어 보니:
DB에 `post_inspection_buffer_slot`이라는 테이블이 있는데, 이게 실제 공정 라인에서 검사 완료된 PCB 배치가 다음 단계(SMD 인계나 최종 출고)로 넘어가기 전 잠시 머무는 16개의 물리적 대기 공간을 나타냅니다. 마치 공항 수하물 컨베이어 옆에 있는 16개의 카트 자리 같은 거예요.

이 16개 자리를 만드는 코드(migration)는 분명히 있었는데, 실제 서버 DB에는 단 한 번도 실행된 적이 없었습니다. 왜냐하면 그 코드가 *"DB를 맨 처음 만들 때 딱 한 번만 실행되는"* 방식으로 설계돼 있었는데, 서버는 이미 예전부터 쓰던 DB를 계속 재사용하고 있었거든요. 그래서 시스템은 **"카트 자리가 0개"**라고 생각하고 있었던 겁니다.

당장은 사람이 직접 SQL을 16번 입력해서 응급 처치했지만, 팀은 여기서 훨씬 중요한 걸 깨달았습니다:

> **"앞으로 DB 구조를 바꿀 일이 또 생길 텐데, 그때마다 사람이 서버에 SSH로 들어가서 수동으로 처리해야 한다면, 언젠가 또 똑같이 조용히 빠뜨리는 사고가 난다."**

이게 오늘(8/18) Phase 1~3을 진행하게 된 근본 이유입니다. **"DB 구조 변경을 자동으로, 안전하게, 빠뜨림 없이 배포하는 시스템"**을 만드는 것.

---

## 1. 왜 "그냥 자동으로 다 실행하면 되잖아?"가 아니었나

가장 쉬운 해결책은 *"배포할 때마다 모든 migration 파일을 순서대로 실행해버리면 되지 않나?"*입니다.

그런데 조사해보니 기존에 쌓여있던 11개 migration 파일 중에는:
* 컬럼을 삭제하는(DROP) 파일
* 예전 변경을 되돌리는(rollback) 용도로 만든 파일
* 이미 다른 파일과 내용이 겹치는 파일

이런 것들이 섞여 있었습니다. 이걸 자동으로 다 실행해버리면, 오히려 운영 중인 실제 데이터를 실수로 지워버릴 위험이 있었어요. **"실행 안 해서 문제"**였는데 **"무작정 다 실행해서 더 큰 문제"**가 되는 걸 막아야 했습니다.

그래서 첫 단계로, AWS 서버 DB를 읽기 전용으로 직접 조사해서 *"지금 당장 적용해야 할 안전한 변경사항이 실제로 있는가"*부터 확인했습니다. 결과는 **0개**였습니다 — 다행히 지금 당장 급한 건 없었고, 그래서 **"당장 불을 끄는 작업"**이 아니라 **"앞으로 이런 사고가 다시 안 나게 만드는 예방 시스템"**으로 방향을 잡을 수 있었습니다.

---

## 2. Phase 1 — "체크리스트를 만들자" (schema_migrations + 실행기)

비유로 설명하면: 택배 창고에 물건을 넣는데, 어떤 상자를 이미 넣었는지 기록하는 장부가 없다고 생각해보세요. 매번 "이거 넣었던가?" 헷갈리고, 실수로 두 번 넣거나 아예 빠뜨릴 수 있습니다.

`schema_migrations`는 이 장부 테이블입니다. "이 변경사항은 언제 적용됐다"를 기록합니다. 그리고 이 장부를 보고 "아직 안 넣은 상자만 골라서 넣어주는" 프로그램(Python 실행기)을 만들었습니다.

### 🛠️ 이 실행기는 세 가지 모드로 동작합니다:
* **`plan`** : "지금 뭘 넣어야 하는지 미리 보여만 줘" (아무것도 안 건드림, 순수 조회)
* **`apply`** : "실제로 넣어"
* **`verify`** : "방금 넣은 게(또는 이미 있던 게) 정말 제대로 들어갔는지 확인해줘"

---

### ⚠️ 여기서 발견한 진짜 까다로운 문제

DB 구조를 바꾸는 명령(컬럼 추가 등)은 실행되는 순간 되돌릴 수 없이 즉시 확정됩니다. 문제는, **"구조를 바꾸는 것"**과 **"장부에 기록하는 것"**이 하나로 묶이지 않는다는 겁니다.

> **비유:** 카드로 결제했는데, 결제는 이미 승인됐지만(돈은 이미 빠져나감) 영수증 프린터가 그 순간 종이가 떨어져서 영수증이 안 나온 상황을 상상해보세요. 나중에 "저 결제한 적 없는데요?"라고 하면, 가게 입장에선 영수증이 없으니 다시 결제하라고 할 수도 있죠. 근데 실제로는 이미 결제된 거라, 두 번 결제되는 사고가 나는 겁니다.

DB도 똑같습니다: "컬럼 추가"는 성공했는데, 딱 그 직후에 "장부에 기록"하기 전에 네트워크가 끊기거나 서버가 죽으면, 실제로는 이미 바뀐 DB인데 장부에는 "안 바뀜"으로 남는 상황이 생깁니다. 다음번에 무심코 또 실행하면 에러가 나거나 꼬일 수 있어요.

그래서 만든 게 **"영수증이 없어도, 결제가 실제로 됐는지 확인해서 뒤늦게 영수증을 발급해주는"** 로직입니다(`RECONCILED`).  
*"장부엔 없지만, 실제로 확인해보니 이미 적용돼 있네? 그럼 다시 하지 말고 장부에만 뒤늦게 적어두자"*는 겁니다. 이걸 실제로 DB를 강제로 중간에 끊어서 재현해보고, 제대로 복구되는지까지 검증했습니다.

---

## 3. Phase 2 — "16개 대기 공간을 안전하게 채우자" (Buffer Bootstrap)

이건 정확히 8/16 사고를 근본적으로 재발 방지하는 작업입니다.

목표는 단순합니다: **"16개 슬롯이 항상 존재하게 만들어라. 그런데 이미 실제로 쓰이고 있는 슬롯은 절대 건드리지 마라."**

> **비유:** 주차장에 번호 표지판(1번~16번)이 없는 자리가 있으면 표지판을 새로 세워주되, 이미 차가 주차돼 있는 자리는 절대 건드리지 않는 겁니다. 만약 실수로 "모든 자리를 빈 상태로 초기화"해버리면, 실제로 차(진행 중인 배치)가 있는데도 시스템은 "여기 비었네"라고 착각해서 다른 차를 그 위에 덮어 배정하는 대형 사고가 날 수 있습니다.

그래서 슬롯의 상태(비어있음/점유중), 어떤 배치가 있는지, 언제부터 있었는지 같은 정보는 절대 덮어쓰지 않도록 코드를 짜고, 이걸 실제로 "누군가 슬롯을 쓰고 있는 상태"를 인위적으로 만든 뒤 다시 실행해봐도 그대로 유지되는지까지 확인했습니다.

---

## 4. Phase 3 — "이 안전장치를 실제 배포 과정에 연결하자"

Phase 1, 2까지는 "안전한 도구"를 만든 거고, Phase 3은 이 도구가 실제로 AWS 서버에 배포될 때마다 자동으로 작동하도록 연결하는 작업입니다. 그리고 여기서 오늘 가장 중요한 발견이 나왔습니다.

### 🚨 발견①: "노트북에선 되는데 실제 배포 환경에선 100% 죽는" 버그
코드 안에서 다른 코드를 불러올 때 쓰는 "주소 표기 방식"이 있었는데, 이게 테스트 환경(CI)에서만 통하는 주소였습니다.

* **비유:** "옆집 기준으로 세 번째 집"이라고 주소를 알려주면, 항상 옆집에서 출발하는 사람에겐 통하지만, 실제 배송 기사(진짜 배포 환경)는 옆집이 어딘지 모르니 못 찾아갑니다. 실제 도로명 주소로 바꿔줘야 누구나 찾아올 수 있죠.

이 버그는 에러가 확 나기 때문에 배포하자마자 바로 알아차릴 수 있는, 그나마 덜 위험한 종류였습니다.

### 💣 발견②: 훨씬 위험한, "에러도 안 나고 조용히 아무 일도 안 하는" 버그
새로 만든 DB 변경사항 파일들을 담는 폴더가 있는데, 이 폴더를 실제 배포용 이미지 안에 복사해 넣는 걸 깜빡했습니다. 그러면 어떻게 되냐면 — 시스템이 "적용할 변경사항을 찾아봤는데, 0개 있네요"라고 에러 없이 태연하게 보고합니다.

* **비유:** 장보러 갈 때 쓸 목록을 냉장고에 자석으로 붙여뒀는데, 정작 가방엔 안 넣고 나간 상황이에요. 마트에서 "뭐 사야하지?" 하고 가방을 열어봤는데 목록이 없으면, "어? 살 게 없나보다" 하고 그냥 집에 옵니다. 에러가 없으니 아무도 눈치를 못 채고, 나중에 "왜 그때 산 게 하나도 없지?"라고 뒤늦게 알게 되는 거죠.

이게 훨씬 위험한 이유는, 8/16 사고와 똑같은 패턴 — **"바뀌었어야 하는데 조용히 안 바뀜"** — 이 새로 만든 안전장치 안에서도 그대로 재현될 뻔했다는 겁니다. 실제 배포 환경으로 직접 재현 테스트를 안 했으면 절대 못 잡았을 버그였습니다.

### 💾 그 외 — 배포 직전 자동 백업
DB를 바꾸기 직전에 자동으로 백업을 떠두는 스크립트도 만들었습니다. 게임하기 전에 자동 저장을 해두는 것과 같습니다 — 뭔가 잘못돼도 그 시점으로 되돌릴 수 있게요.

---

## 5. 전체를 한 문장으로 정리하면

> **"검사 완료된 PCB가 실제 공정 라인에서 다음 단계로 넘어가지 못하고 멈췄던 사고(8/16) → 그 원인이 'DB 구조 변경이 서버에 조용히 누락되는 구조적 문제'였다는 걸 발견 → 이런 사고가 다시는 조용히 재발하지 않도록, 체크리스트(Phase 1) + 안전한 데이터 채우기(Phase 2) + 실제 배포 파이프라인 연결·검증(Phase 3)까지 이어지는 하나의 안전 배포 체계를 오늘 하루 동안 설계·구현했다."**

발표에서 쓰신다면, "AI가 불량을 잘 잡아낸다" 못지않게 "이 시스템이 실제로 안정적으로 배포되고, 조용히 망가지는 사고를 미리 잡아낸다"는 것도 엔지니어링 완성도를 보여주는 좋은 포인트가 될 것 같습니다.