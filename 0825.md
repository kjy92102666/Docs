
# 2026-08-25 (화) 작업 로그

## 오늘 진행한 내용

### 1. 기존 진행 상황 리마인드

- 인계 문서 기반으로 현재 상태 재확인
- `HelloServlet` → `index.jsp` forward 흐름 (Servlet→JSP 데이터 전달 검증 완료 상태) 재확인
- 다음 과제로 `/hello?name=홍길동` 형태의 **URL 파라미터 처리**를 진행하기로 함

### 2. 파라미터 처리 과제 착수 (진행 중, 미완성)

- 기존 `doGet` 구조 유지 방향 확인:

java

```java
  protected void doGet(HttpServletRequest request, HttpServletResponse response)
          throws ServletException, IOException {
      request.setAttribute("message", "...");
      RequestDispatcher rd = request.getRequestDispatcher("/index.jsp");
      rd.forward(request, response);
  }
```

- 힌트로 받은 내용:
    - `request.getParameter("파라미터명")` → 항상 `String` 또는 파라미터 없으면 `null` 반환
    - **고민해야 할 포인트 2가지**
        1. `name` 파라미터가 없을 때(null) NPE 방지 처리를 어떻게 할지
        2. GET 파라미터 한글 인코딩 이슈 — `setCharacterEncoding()`은 POST에만 적용되고 GET에는 안 먹을 수 있음 → Tomcat 8.5 `server.xml`의 `URIEncoding="UTF-8"` 설정 여부 확인 필요 (미확인, 다음에 체크할 것)
- index.jsp에서 EL(`${message}`) vs 스크립틀릿(`<%= %>`) 중 어떤 방식 쓰고 있었는지 확인 필요 (미확인)

> **다음에 이어서 할 것**: 위 두 가지 고민(null 처리, 인코딩) 반영해서 `doGet` 직접 작성 → 리뷰 요청

### 3. 환경 재부팅 이슈 점검

- 컴퓨터 재부팅 관련 문의 → 재부팅 자체로 인한 별도 설정 불필요함을 확인
- **재부팅 후 유지되는 것**
    - Docker 볼륨(`hcg-oracle-data`) 기반 HR 스키마 데이터, 계정 정보
    - ASIS/React 프로젝트 파일, Eclipse 설정, `ojdbc7.jar` 배치 상태
- **재부팅 후 반드시 다시 해줘야 하는 것**
    - Oracle 컨테이너 자동시작 안 함으로 설정되어 있으므로:

```
    docker start practice-oracle-xe
```

```
직접 실행 필요
```

- **재개 순서 정리**
    1. Docker Desktop 실행 대기
    2. `docker start practice-oracle-xe`
    3. `docker ps`로 컨테이너 정상 기동(헬스체크 완료) 확인
    4. Eclipse에서 `hcg-asis` 프로젝트 열고 Tomcat 8.5 기동 (kcarbon과 포트 충돌 주의, 동시 실행 금지)
    5. 필요 시 `cd frontend && npm run dev` (React, localhost:5173)

## 미해결 / 다음 확인 필요 사항

- [ ]  Tomcat 8.5 `server.xml`의 `URIEncoding="UTF-8"` 설정 여부 확인
- [ ]  index.jsp에서 EL vs 스크립틀릿 사용 방식 확인
- [ ]  `/hello?name=홍길동` doGet 파라미터 처리 코드 직접 작성 → 리뷰 요청
- [ ]  (재부팅 후) `docker start practice-oracle-xe` 실행 및 `docker ps` 확인

## 전체 로드맵 대비 현재 위치

1. ~~Servlet 파라미터 처리 연습~~ ← **현재 진행 중**
2. Model1 뼈대 CRUD 화면 1개 완성 (미착수)
3. Oracle Procedure/Function 작성 연습 (미착수)
4. Model2(자체 Front Controller) 리팩토링 (미착수)
5. Spring MVC/Boot TOBE 전환 (미착수)
6. JSP/jQuery + React 과도기 혼재 구조 실습 (미착수)
7. DB 성능 연습 - 조인/서브쿼리/실행계획/인덱스 튜닝 (미착수)

## 일정 메모

- 화(오늘)/수/목 학습, 금요일 면접 — 남은 시간 관리 필요