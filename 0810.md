

# 목표는 **"GitHub Actions 버튼 하나로 서버에 배포되고, 브라우저에서 응답이 보인다"**

1. 팀 공유 (아침, 어제 드린 문구 참고)
2. `ci-practice` 연습 저장소 생성 — `/health`만 있는 FastAPI
3. 512MB 인스턴스에 SSH → Docker 설치
4. 손으로 배포 → 외부 브라우저에서 확인 (**여기서 방화벽에 막힙니다. Lightsail 콘솔 네트워킹 탭에서 포트 개방 필요**)
5. SSH 키 → GitHub Secrets → `workflow_dispatch` 배포 워크플로우
6. 틈날 때 ①번(torch 없이 pytest 수집) 조사

**4번이 오늘보다 훨씬 많이 막힐 겁니다.** 시간을 넉넉히 잡으시고, 막히면 바로 물어봐 주세요.




# CD,AWS 배포 과정
### 1. 팀 공유 (5분)

어제 정리한 문구 그대로 보내시면 됩니다. 추가할 내용 없습니다.
```
**CI 도입 진행 상황 공유**

`feature/ci-setup` 브랜치에서 GitHub Actions CI를 구성했습니다. 아직 dev 병합 전이고, 화요일에 PR 올릴 예정입니다.

**추가된 것**

- `.github/workflows/ci.yml` — feature 브랜치 push 시 백엔드 pytest + 프론트 테스트/빌드 자동 실행
- `backend/requirements-dev.txt` — pytest 등 테스트 전용 의존성 분리

**발견한 문제와 수정**

- 프론트 `npm test`가 CI(Linux)에서 449개 중 **8개만 실행되고도 통과**하고 있었습니다. `package.json`의 `test/**/*.test.js` glob이 Linux 셸에서 재귀 확장되지 않는 문제였습니다. Windows에서는 아예 실행 실패였고요.
- `node --test`로 변경해서 해결했습니다. 이제 474개가 실행됩니다.
- `src/features/process-status/components/factoryMapLayout.test.js`(25개)가 그동안 한 번도 실행되지 않고 있었는데, 이제 포함됩니다.
- `agvControl.test.js`의 메타 테스트가 기존 glob 문자열을 검사하고 있어서 함께 수정했습니다.

**팀원 영향**

- 앞으로 `npm test` 결과가 8개 → 474개로 바뀝니다
- 백엔드는 282 passed / 2 skipped로 정상 확인
- 당분간 CI 실패가 병합을 막지는 않도록 설정했습니다

혹시 `package.json`이나 메타 테스트 변경에 의견 있으시면 알려주세요.
```
### 2. `ci-practice` 연습 저장소 (30분)

#### 2-1. 저장소 생성

GitHub에서 새 저장소 생성 (private, README 포함).

bash

```bash.   
kjy92102666
git clone https://github.com/kjy92102666/ci-practice.git
cd ci-practice
```

#### 2-2. 최소 FastAPI

bash

```bash
mkdir app
```

`app/main.py`:

python

```python
from fastapi import FastAPI

app = FastAPI()

@app.get("/health")
def health():
    return {"status": "ok"}
```

`requirements.txt`:

```
fastapi==0.115.0
uvicorn==0.32.0
```

`Dockerfile`:

dockerfile

```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY app/ ./app
EXPOSE 8000
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

#### 2-3. 로컬 확인

bash

```bash
docker build -t ci-practice .
docker run -p 8000:8000 ci-practice
```

브라우저에서 `http://localhost:8000/health` → `{"status":"ok"}` 확인되면 push.

bash

```bash
git add .
git commit -m "init: minimal FastAPI health check"
git push
```

### 3. 512MB 인스턴스 준비 (30분~1시간)

#### 3-1. SSH 접속

Lightsail 콘솔 → 인스턴스 → **연결(Connect)** 탭 → 브라우저 기반 SSH로 먼저 접속해보세요. 로컬 터미널 SSH는 나중에 키 설정하면서 다시 씁니다.

#### 3-2. Docker 설치 (Ubuntu 기준)

bash

```bash
sudo apt-get update
sudo apt-get install -y ca-certificates curl gnupg

sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
sudo chmod a+r /etc/apt/keyrings/docker.gpg

echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
  $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

sudo apt-get update
sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin
```

#### 3-3. sudo 없이 docker 쓰기 (중요)

bash

```bash
sudo usermod -aG docker $USER
```

**여기서 반드시 로그아웃 후 재접속하세요.** 그룹 반영이 즉시 안 됩니다. 브라우저 SSH면 창을 닫고 다시 열면 됩니다.

#### 3-4. 확인

bash

```bash
docker run hello-world
```

`sudo` 없이 성공하면 끝입니다. 여기서 permission denied가 나오면 재접속을 안 한 것입니다.

### 4. 손으로 배포 (여기가 오늘의 관문)

#### 4-1. 서버에서 clone & 빌드

bash

```bash
git clone https://github.com/<본인계정>/ci-practice.git
cd ci-practice
docker build -t ci-practice .
docker run -d -p 8000:8000 --restart always --name ci-practice ci-practice
```

#### 4-2. 서버 안에서 먼저 확인

bash

```bash
curl http://localhost:8000/health
```

`{"status":"ok"}`가 나오면 컨테이너 자체는 정상입니다. **여기서 안 되면 방화벽 문제가 아니라 코드/빌드 문제**이니 구분해서 접근하세요.

#### 4-3. 외부에서 접속 (여기서 막힙니다)

서버 안에서는 되는데 브라우저에서 `http://<고정IP>:8000/health`가 안 열리면 방화벽입니다.

**Lightsail 콘솔 → 해당 인스턴스 → 네트워킹(Networking) 탭 → IPv4 방화벽**

`Add rule` → Custom → TCP → 포트 `8000` → Create.

몇 초 기다린 후 다시 브라우저에서 시도하세요.

**흔한 함정 두 가지**

- 방화벽은 Lightsail 콘솔에만 있는 게 아니라 서버 안 `ufw`도 막을 수 있습니다. `sudo ufw status`로 확인, 활성화 상태면 `sudo ufw allow 8000`
- 고정 IP를 아직 안 할당했다면 인스턴스 재시작 시 IP가 바뀝니다. **네트워킹 탭에서 고정 IP를 먼저 붙이세요.**

#### 4-4. 완료 조건

브라우저에서 `http://<고정IP>:8000/health` → `{"status":"ok"}` 표시.

### 5. SSH 배포 자동화 (1~1.5시간)

#### 5-1. 배포 전용 SSH 키 생성 (로컬 PC에서)

bash

```bash
ssh-keygen -t ed25519 -f ci-practice-deploy -N ""
```

`ci-practice-deploy`(개인키), `ci-practice-deploy.pub`(공개키) 두 파일이 생깁니다.

#### 5-2. 서버에 공개키 등록

bash

```bash
cat ci-practice-deploy.pub
```

출력된 내용을 복사해서, 서버에 SSH 접속한 상태에서:

bash

```bash
mkdir -p ~/.ssh
echo "<복사한 공개키>" >> ~/.ssh/authorized_keys
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys
```

#### 5-3. 로컬에서 개인키로 접속 테스트

bash

```bash
ssh -i ci-practice-deploy ubuntu@<고정IP>
```

여기서 성공해야 GitHub Actions에서도 성공합니다.

#### 5-4. GitHub Secrets 등록

저장소 → Settings → Secrets and variables → Actions → New repository secret

|Name|Value|
|---|---|
|`SSH_HOST`|고정 IP|
|`SSH_USER`|`ubuntu`|
|`SSH_PRIVATE_KEY`|`ci-practice-deploy` 파일 **전체 내용** (`-----BEGIN...` 부터 `-----END...`까지 그대로)|

**개인키 등록 시 흔한 실수**: 파일을 열어서 복사할 때 마지막 개행이 빠지면 인증이 실패합니다. `cat ci-practice-deploy`로 출력한 걸 통째로 복사하세요.

#### 5-5. 워크플로우 작성

`.github/workflows/deploy.yml`:

yaml

```yaml
name: deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd ci-practice
            git pull
            docker build -t ci-practice .
            docker stop ci-practice || true
            docker rm ci-practice || true
            docker run -d -p 8000:8000 --restart always --name ci-practice ci-practice
            sleep 2
            curl -f http://localhost:8000/health
```

`curl -f`가 실패하면 워크플로우 자체가 실패로 표시됩니다. 배포 성공 여부를 자동으로 확인하는 역할입니다.

#### 5-6. 실행

bash

```bash
git add .github/workflows/deploy.yml
git commit -m "ci: add manual deploy workflow"
git push
```

GitHub 저장소 → **Actions** 탭 → `deploy` 워크플로우 선택 → **Run workflow** 버튼 클릭.

성공하면 초록불, 그리고 브라우저에서 다시 `/health` 확인.

### 6. torch 조사 (틈날 때, 순서 안 정해도 됨)

앞서 드린 프롬프트 그대로 실행하시면 됩니다.

```
[중요] 이건 조사 요청이야. 저장소의 어떤 파일도 수정하지 마.

backend/tests의 282개 테스트를 torch/ultralytics 없이 수집할 수 있는지 확인해줘.

1. /tmp 등 저장소 밖에 임시 가상환경 생성
2. requirements.txt에서 torch, ultralytics, opencv 계열을 제외하고 설치
3. pytest --collect-only backend/tests 실행

알려줄 것:
- 수집이 성공하는지 실패하는지
- 실패하면 어떤 파일의 어떤 import 때문인지
- 성공시키려면 어떤 수정이 필요한지 (제안만, 적용은 하지 마)
```

이 결과가 화요일 Dockerfile의 베이스 이미지 선택에 직결됩니다.

### 오늘의 완료 조건 (다시 한번)

**버튼 하나 눌러서 GitHub Actions가 서버에 배포하고, 브라우저에서 응답이 보인다.**

이게 되면 화요일에는 이 파이프라인 구조를 그대로 가져다가 `ci-practice`를 실제 프로젝트로, 512MB를 4GB로 바꾸기만 하면 됩니다.

### 자주 막히는 지점 정리 (미리 참고)

|증상|원인|해결|
|---|---|---|
|`docker: permission denied`|그룹 반영 안 됨|재로그인|
|서버 안에선 되는데 외부에서 안 됨|Lightsail 방화벽|네트워킹 탭 포트 개방|
|방화벽 열었는데도 안 됨|`ufw` 별도 차단|`sudo ufw allow 8000`|
|Actions에서 SSH 인증 실패|개인키 복사 시 개행 누락|`cat`으로 전체 복사|
|`git pull` 시 권한 오류|서버의 clone이 https, private repo|PAT 사용 또는 저장소를 public으로|






# # 배포 파이프라인 연습 결과 (2026-08-10)

> **목표**: GitHub Actions 버튼 하나로 서버에 배포되고, 브라우저에서 응답이 보인다. **결과**: ✅ 달성 **범위**: 별도 연습 저장소(`KDT-ci-practice`) + 기존 512MB Lightsail 인스턴스(`pcb-mes-db-dev`) 사용, 실제 프로젝트 저장소/서버 무영향

---

## 1. 목적

화요일(8/11)부터 실제 프로젝트를 배포하기 전에, **배포 파이프라인 전체 흐름을 저risk 환경에서 먼저 겪어본다.** 연습 저장소와 무료 체험 중인 512MB 인스턴스를 사용해 비용 0, 실패해도 되돌릴 것이 없는 조건에서 진행했다.

---

## 2. 준비물

|항목|내용|
|---|---|
|연습 저장소|`github.com/kjy92102666/KDT-ci-practice` (개인 계정, 실제 프로젝트와 완전 분리)|
|연습 서버|Lightsail `pcb-mes-db-dev`, 512MB RAM / 2vCPU / 20GB SSD, 서울 리전|
|최소 앱|`/health`만 있는 FastAPI (uvicorn)|

---

## 3. 진행 과정 및 발견 사항

### 3-1. 최소 애플리케이션 작성

- `app/main.py` — `/health` 엔드포인트 하나
- `requirements.txt` — fastapi, uvicorn
- `Dockerfile` — python:3.11-slim 베이스

**이슈**: Windows `notepad`로 `Dockerfile` 저장 시 자동으로 `Dockerfile.txt`가 됨 → `ren Dockerfile.txt Dockerfile`로 확장자 제거

**이슈**: 로컬 PC에 Docker Desktop 미설치 확인됨 → 로컬 빌드 단계 스킵, 서버에서 직접 빌드하는 방식으로 진행 (문제 없음)

**이슈**: git 최초 커밋 시 `Author identity unknown` → `git config --global user.name/user.email` 설정 후 재시도

### 3-2. GitHub push

```bash
git add .
git commit -m "init: minimal FastAPI health check"
git push -u origin main
```

정상 완료.

### 3-3. Lightsail 서버 준비

- 브라우저 SSH로 `pcb-mes-db-dev` 접속
- Docker 설치: `sudo apt install -y docker.io` (Ubuntu 기본 패키지 방식)
- `sudo usermod -aG docker $USER` → **재로그인 필요** (그룹 반영은 즉시 적용 안 됨)
- `docker run hello-world`로 sudo 없이 동작 확인

### 3-4. ⚠️ 핵심 이슈: 메모리 부족 (OOM)

**증상**: `docker build` 중 `pip install` 단계에서 `Killed`, exit code 137

**원인**: 512MB RAM으로 fastapi/uvicorn 등 소규모 패키지 설치도 커널이 강제 종료(OOM Kill)

**대응**: 스왑 파일 1GB 추가로 해결

```bash
sudo fallocate -l 1G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```

→ 재시도 시 빌드 성공 (`Successfully built`, `Successfully tagged ci-practice:latest`)

**시사점**: 512MB는 순수 FastAPI+uvicorn 빌드조차 스왑 없이는 실패하는 사양이다. 화요일 실제 프로젝트(torch·YOLO·MariaDB 포함, 훨씬 무거움)를 위해 **4GB 인스턴스로 결정한 판단이 타당함**을 실증적으로 확인했다.

### 3-5. 컨테이너 실행 및 로컬 확인

```bash
docker run -d -p 8000:8000 --restart always --name ci-practice ci-practice
curl http://localhost:8000/health
# → {"status":"ok"}
```

서버 내부에서는 정상 동작 확인.

### 3-6. ⚠️ 이슈: 외부 접속 실패 (방화벽)

**증상**: 브라우저에서 `http://<고정IP>:8000/health` → `ERR_CONNECTION_TIMED_OUT`

**원인 1**: Lightsail 콘솔의 IPv4 방화벽에 8000번 포트 규칙 자체가 없었음 **원인 2**: 규칙 추가 시 "소스 IP 주소"를 채우지 않아 저장 버튼이 비활성화됨 (경고 문구로 확인)

**대응**:

1. 네트워킹 탭 → 규칙 추가 → Custom / TCP / 8000
2. 소스 IP를 `0.0.0.0/0` (모든 IPv4)로 설정 후 저장

→ 재시도 시 브라우저에서 `{"status":"ok"}` 정상 확인 (**오늘의 관문 통과**)

### 3-7. SSH 배포 자동화

**키 생성 및 등록**

```bash
# 로컬
ssh-keygen -t ed25519 -f ci-practice-deploy -N ""

# 서버
mkdir -p ~/.ssh
echo "<공개키>" >> ~/.ssh/authorized_keys
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys
```

**접속 테스트**: 로컬에서 개인키로 비밀번호 없이 접속 성공

**이슈**: `Secrets and variables`를 계정 설정(프로필)에서 찾다가 못 찾음 → **저장소 자체의 Settings**(계정 설정과 다른 위치)에 있음을 확인 → `github.com/<계정>/<저장소>/settings/secrets/actions`

**등록한 Secrets**

|Name|Value|
|---|---|
|`SSH_HOST`|Lightsail 고정 IP|
|`SSH_USER`|`ubuntu`|
|`SSH_PRIVATE_KEY`|개인키 파일 전체 내용|

**워크플로우 작성**: `.github/workflows/deploy.yml`

```yaml
name: deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd KDT-ci-practice
            git pull
            docker build -t ci-practice .
            docker stop ci-practice || true
            docker rm ci-practice || true
            docker run -d -p 8000:8000 --restart always --name ci-practice ci-practice
            sleep 2
            curl -f http://localhost:8000/health
```

**이슈**: 파일 생성 후 Actions 탭에 워크플로우가 안 보임 → `.github/` 폴더가 push 안 된 상태였음 (`git status`에서 Untracked 확인) → `git add . && git commit && git push` 후 정상 인식

### 3-8. 최종 실행 결과

GitHub Actions → `deploy` 워크플로우 → **Run workflow** 수동 실행

```
Run #1 · succeeded in 17s

out: Successfully built 9132e98b2378
out: Successfully tagged ci-practice:latest
out: {"status":"ok"}
✅ Successfully executed commands to all host.
```

**버튼 클릭 → SSH 접속 → git pull → docker build → 컨테이너 교체 → 자체 헬스체크 검증 → 성공 표시** 전 과정 자동화 확인 완료.

---

## 4. 검증된 전체 파이프라인

```
로컬 코드 push
   ↓
GitHub Actions "Run workflow" 클릭
   ↓
GitHub 러너가 SSH로 서버 접속 (SSH_PRIVATE_KEY)
   ↓
서버: git pull → docker build → 기존 컨테이너 정지/삭제 → 신규 컨테이너 실행
   ↓
서버: curl로 자체 /health 호출 (curl -f 로 실패 시 워크플로우 실패 처리)
   ↓
Actions 화면에 성공/실패 표시
```

---

## 5. 발견한 문제와 해결 방법 (재사용 가능한 지식)

|문제|원인|해결|
|---|---|---|
|`docker` 명령 인식 안 됨|Docker Desktop 미설치 (로컬)|로컬 빌드 생략, 서버 빌드로 진행|
|`Author identity unknown`|git 사용자 정보 미설정|`git config --global` 설정|
|`Dockerfile.txt` 생성됨|notepad 자동 확장자 추가|`ren`으로 확장자 제거|
|`docker build` 중 `Killed` (exit 137)|512MB RAM 부족 (OOM)|스왑 1GB 추가|
|외부 브라우저 `ERR_CONNECTION_TIMED_OUT`|Lightsail 방화벽에 포트 미등록|네트워킹 탭에서 TCP 8000 규칙 추가|
|방화벽 규칙 저장 안 됨|소스 IP 주소 미입력|`0.0.0.0/0` 입력|
|Secrets 메뉴를 못 찾음|계정 설정과 저장소 설정 혼동|저장소 Settings 안에 위치 확인|
|Actions에 워크플로우 안 보임|`.github/` push 누락|`git add/commit/push` 재확인|

이 목록은 화요일 실제 4GB 인스턴스 배포 시 **동일하게 재발 가능한 지점**이므로 그대로 참고한다.

---

## 6. 화요일(8/11) 재사용 매핑

|오늘 (연습)|화요일 (실전)|변경 사항|
|---|---|---|
|`KDT-ci-practice` 저장소|`pcb-defects-mes`|실제 앱 코드|
|512MB 인스턴스|4GB 인스턴스 (신규 생성)|사양 상향|
|`/health`만 있는 FastAPI|FastAPI + YOLO + MariaDB + 챗봇|Docker Compose 3개 서비스|
|스왑 1GB로 겨우 빌드|torch 포함 베이스 이미지 사용 예정|빌드 실패 리스크 사전 차단|
|소스 IP 전체 개방|필요한 포트만 (80/443) 제한 예정|보안 강화|
|SSH 키/Secrets 절차|동일 절차 재사용|신규 키 생성만 필요|

---

## 7. 자원 정리 상태

|자원|처리|
|---|---|
|`pcb-mes-db-dev` (512MB)|**유지** — 무료 체험 기간(~10월 중순) 내, 비용 0. 추가 연습용 샌드박스로 보존|
|`ci-practice` 컨테이너|인스턴스 내 계속 실행 중 (필요 시 `docker stop ci-practice`로 중지 가능)|
|Lightsail 방화벽 8000 포트|`0.0.0.0/0`으로 열려 있음 (연습 목적상 유지, 필요 시 규칙 삭제 가능)|
|`KDT-ci-practice` 저장소|유지 (향후 재연습 시 재사용)|

---

## 8. 남은 조사 (화요일 착수 전 필요)

**① torch 없이 pytest 수집 가능 여부** — 미완료, 별도 프롬프트로 진행 예정

목적: CI에서 torch 설치를 조건부로 생략해 실행 시간을 줄일 수 있는지 판단. 결과에 따라 화요일 Dockerfile의 베이스 이미지 선택 및 CI 잡 분리 여부가 결정된다.

---

## 9. 이틀간(8/9~8/10) 종합 성과

- CI 구축 완료 (backend 282 passed / frontend 474 passed, 약 2분)
- 프론트 테스트 커버리지 문제 발견 및 해결 (8개 → 474개 실행)
- 49커밋 규모 브랜치 병합 정리 및 PR 리뷰 프로세스 준수
- **배포 파이프라인 전체를 저risk 환경에서 1회 완주, 재발 가능 문제 8건 사전 확보**

화요일부터는 이번에 확보한 절차와 문제-해결 매핑을 바탕으로 실제 프로젝트 배포를 진행한다.