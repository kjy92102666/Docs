# 🚚 AGV 이동규칙

> [!abstract] AGV 운송 규칙 전체 정리

## 1. 기본 전제

- 계획 수량은 최대 **500 PCB**
    
- 배치 수량도 최대 **500 PCB**
    
- AGV 최대 적재량은 **500 PCB**
    
- 배치 하나를 AGV 한 대가 한 번에 운송
    
- 검사 시작 후에는 **중단·일시정지·취소 없음**
    
- YOLO 적용 후 실제 검사 속도에 따라 검사 진행률 반영
    
- UI는 다른 팀원이 담당
    
- AGV는 실제 제어가 아닌 **상태전이 시뮬레이션**
    

> [!warning] 구현 시 변경 필요  
> 현재 AGV 코드의 적재 한도는 아직 **320**이므로 실제 구현 시 **500**으로 변경해야 합니다.

### 물리 공간 구분

대기존은 두 종류를 구분해야 합니다.

- **AGV 대기존**  
    → 빈 AGV가 대기하는 공간
    
- **검사 전 배치 대기존**  
    → 등록된 PCB 배치가 라인 투입을 기다리는 공간
    

> [!warning] UI 반영 필요  
> 현재 맵에는 AGV 대기존은 있지만 검사 전 배치 대기존은 없습니다. UI 담당자에게 별도 구역 추가를 요청해야 합니다.

기존 공간까지 포함하면:

- AGV 대기존
    
- 검사 전 배치 대기존
    
- 검사 라인
    
- 검사 후 적재공간
    
- 최종 적재공간
    
- 충전 구역
    

---

## 2. 계획 등록

계획은 현재 라인 상태나 모델과 관계없이 등록할 수 있습니다.

> [!info] 상태 흐름  
> **계획 등록**  
> → `PLANNED`  
> → AGV 이동 없음  
> → 취소 가능

구체적인 검사 시간이 없으므로 라인이 현재 검사 중이어도 미래 계획은 등록할 수 있습니다.

---

## 3. 배치 등록

### 라인이 비어 있는 경우

> [!info] 상태 흐름  
> **배치 등록**  
> → `CREATED`  
> → AGV 운송 작업 생성  
> → `LINE_ASSIGNED`  
> → 검사 라인으로 직접 운송  
> → 검사 시작 전 대기

라인이 비어 있으면 어떤 모델이든 배치 등록할 수 있고, 해당 모델이 라인의 현재 검사 모델이 됩니다.

### 같은 모델이 검사 중인 경우

요청하신 규칙을 적용합니다.

> [!info] 같은 모델 배치 등록  
> **라인에서 모델 A 검사 중**  
> → 모델 A 계획의 배치 등록 가능  
> → 배치 상태 `CREATED`  
> → AGV가 검사 전 배치 대기존으로 운송  
> → 라인 호출을 기다림

현재 배치의 검사가 끝나고 라인에서 완전히 회수되면:

> [!info] 다음 배치 투입  
> **라인 사용 가능**  
> → 같은 모델 `CREATED` 대기 배치 확인  
> → 라인이 AGV 호출  
> → `LINE_ASSIGNED`  
> → AGV가 대기존에서 검사 라인으로 운송

### 다른 모델이 검사 중인 경우

> [!danger] 다른 모델 배치 등록 제한  
> **라인에서 모델 A 검사 중**  
> → 모델 B 계획 등록은 가능  
> → 모델 B 배치 등록은 불가  
> → 계획은 `PLANNED` 상태로 유지  
> → `LINE_MODEL_CONFLICT` 안내

다른 모델 배치를 미리 생성하지 않으므로 모델 혼입과 변경 대기 처리를 줄일 수 있습니다.

---

## 4. 검사 전 취소

### 계획 취소

> [!info] 계획 상태  
> `PLANNED` → `CANCELLED`

연결된 검사 전 배치가 있다면 배치도 취소하고, 필요한 AGV 반환 작업을 생성합니다.

### 배치 취소

> [!info] 상태 복원  
> **배치:** `CREATED` 또는 `LINE_ASSIGNED` → `CANCELLED`  
> **계획:** `BATCH_CREATED` → `PLANNED`

AGV 상태에 따라:

- **운송 작업 생성 전**  
    → 작업 제거
    
- **검사 전 대기존으로 이동 중**  
    → 원래 입고 위치로 반환
    
- **검사 전 대기존에 도착**  
    → 대기 배치 회수
    
- **검사 라인으로 이동 중**  
    → 운송 중단 후 반환
    
- **검사 라인에 도착했지만 검사 시작 전**  
    → 라인에서 배치 회수
    

> [!warning] 백엔드 수정 필요  
> 현재 백엔드는 `CREATED`만 취소할 수 있으므로, 요구사항대로라면 `LINE_ASSIGNED`도 취소 가능하도록 수정해야 합니다.

---

## 5. 검사 진행

> [!info] 검사 시작 조건  
> `LINE_ASSIGNED`  
> → AGV 라인 도착  
> → 검사 시작 가능

> [!danger] 검사 시작 이후  
> **검사 시작**  
> → `INSPECTING`  
> → 이후 취소·중단 불가

> [!info] YOLO 검사  
> **YOLO 검사**  
> → 실제 완료 PCB 수를 기준으로 진행률 반영

> [!success] 검사 완료  
> **검사 완료**  
> → `INSPECTED`

> [!note] 향후 YOLO 연동  
> 현재는 중간 검사 결과를 실시간으로 전달하지 않지만, YOLO 연동 시 PCB별 완료 결과를 받아 진행률과 AGV 조건에 사용할 예정입니다.

---

## 6. 검사 완료 후

> [!info] 검사 완료 배치 회수  
> `INSPECTED`  
> → 검사 완료 배치 회수 AGV 호출  
> → 검사 후 적재공간으로 운송

> [!success] 운송 완료  
> **운송 완료**  
> → `COMPLETED`  
> → 검사 라인 사용 가능

> [!info] 다음 배치 투입  
> **같은 모델 대기 배치 존재**  
> → 라인이 다음 AGV 호출  
> → 다음 배치 투입

> [!important]  
> 라인에서 기존 검사 완료 배치가 빠지기 전에는 다음 배치를 투입하지 않습니다.

---

## 7. 최종 상태 흐름

```
PLANNED
→ BATCH_CREATED
→ CREATED
   ├─ 라인 사용 가능 → LINE_ASSIGNED
   └─ 같은 모델 검사 중 → 검사 전 배치 대기존

검사 전 취소
→ 배치 CANCELLED
→ 계획 PLANNED 복원

라인 호출
→ LINE_ASSIGNED
→ AGV 운송
→ 라인 도착

검사 시작
→ INSPECTING
→ 취소·중단 불가

검사 완료
→ INSPECTED
→ AGV 회수
→ COMPLETED
→ 라인 사용 가능
```