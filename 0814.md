 - [ ] openai 챗봇 적용 후 클라우드까지 올리기.
 - [ ] ppt기획 초안 프롬프트 걸어놓기.
 - [x] 소스정리 일단 분석 정보(히트맵),리포트 생성까지 적용.
 - [ ] DB 시드 데이터? 넣어서 배치등록부터 가능하게 적용시키기.




# 📋 일일 작업 및 진행 상황 보고

### ✅ 오늘 완료한 작업

- **챗봇 OpenAI Provider 구현 및 로직 추가**
    
    - 응답 시 실제 사용된 Provider 명시 (`ollama` / `openai`)
        
    - 테스트 Provider 및 테스트 격리 문제 수정
        
    - 전체 백엔드 테스트 통과 (`322 passed / 2 skipped`, 총 324개)
        
- **챗봇 기능 커밋 생성**
    
    - 브랜치: `feature/chatbot-openai`
        
    - 커밋: `43f3956`
        
- **분석·리포트 기능 배포**
    
    - 분석 정보(히트맵) 및 리포트 기능 `dev` → `main` 병합
        
    - **AWS 배포 스크립트 수정:** 프론트엔드 미갱신 원인(Git 제외 대상인 `frontend/dist` 및 `npm run build` 누락) 해결
        
    - 배포 스크립트에 `npm ci`, `npm run build`, Docker Compose 재빌드 과정 반영
        
    - 배포 수정 건 `dev` → `main` 병합 완료 (PR #41, 커밋: `fb5afc4`)
        
- **배포 및 반영 확인**
    
    - GitHub Actions Deploy 성공 확인
        
    - AWS 운영 환경(`[http://3.38.222.241/](http://3.38.222.241/)`)에 분석 리뷰 API, 패턴/히트맵 API, Excel/PDF 리포트 내보내기 정상 반영 완료
        

### 🏡 집에서 이어서 진행할 작업 (To-Do)

#### 1. 챗봇 브랜치 수신 및 최신 `dev` 병합

Bash

```
# 원격 브랜치 가져오기
git fetch origin
git switch --track origin/feature/chatbot-openai

# (이미 로컬 브랜치가 있는 경우)
# git switch feature/chatbot-openai
# git pull

# 최신 dev 병합 (충돌 시 해결 후 테스트)
git fetch origin
git merge origin/dev
```

#### 2. Docker Compose 환경변수 추가 (`docker-compose.yml`)

`backend` 서비스 환경변수에 아래 항목 추가:

YAML

```
CHATBOT_PROVIDER: ${CHATBOT_PROVIDER:-ollama}
OPENAI_API_KEY: ${OPENAI_API_KEY:-}
OPENAI_MODEL: ${OPENAI_MODEL:-gpt-4o-mini}
OPENAI_TIMEOUT_SECONDS: ${OPENAI_TIMEOUT_SECONDS:-30}
```

#### 3. `.env.example` 및 AWS `.env` 설정

- **.env.example** (보안을 위해 기본값만 작성):
    
    코드 스니펫
    
    ```
    CHATBOT_PROVIDER=ollama
    OPENAI_API_KEY=
    OPENAI_MODEL=gpt-4o-mini
    OPENAI_TIMEOUT_SECONDS=30
    ```
    
- **AWS 서버 프로젝트 루트 `.env`**:
    
    코드 스니펫
    
    ```
    CHATBOT_PROVIDER=openai
    OPENAI_API_KEY=실제_API_키
    OPENAI_MODEL=gpt-4o-mini
    OPENAI_TIMEOUT_SECONDS=30
    ```
    
    > ⚠️ **주의:** API 키는 Git, 로그, 공용 채널에 절대 노출하지 않을 것!
    

#### 4. 전체 검증 (테스트 & 빌드)

Bash

```
# 백엔드 테스트
backend\.venv\Scripts\python.exe -m pytest backend/tests -v

# 프론트엔드 테스트 및 빌드
cd frontend
npm ci
npm test
npm run build
```

#### 5. PR 생성 및 병합

1. `feature/chatbot-openai` → `dev` PR 생성 (CI 통과 및 팀원 1인 승인 후 병합)
    
2. `dev` → `main` PR 생성 (CI 및 승인 후 병합)
    

#### 6. AWS 재배포 및 최종 운영 확인

- GitHub Actions `main` 브랜치 기준 Deploy 워크플로 실행
    
- `[http://3.38.222.241/health/ready](http://3.38.222.241/health/ready)` 헬스체크 확인
    
- 실제 웹 화면에서 챗봇 질문 전송 후 응답의 `provider`가 `"openai"`로 찍히는지 확인