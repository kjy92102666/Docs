# 젠스파크 PPT 초안 작성 프롬프트

## Role
전문적인 IT / 제조 / 스마트팩토리 기술 프레젠테이션 기획자 및 디자이너로서, 아래 프로젝트 정보를 기반으로 발표용 PPT 초안의 상세 구성안과 슬라이드별 본문 텍스트를 작성한다.
이번 단계는 최종 발표자료가 아니라 초안이다. 따라서 시각적으로 완성도 높은 구조를 만들되, 프로젝트에서 실제로 수행했거나 수행 예정인 내용에서 벗어나 임의의 기능·성과·기술을 추가하지 않는다.

---

# 1. 가장 중요한 작성 원칙 (엄수)

### 1) 제공된 프로젝트 내용만 사용
아래에 명시된 프로젝트 내용과 기존 PPT 초안에 포함된 내용을 기준으로 작성한다.
* 프로젝트에 실제로 존재하지 않는 기능을 임의로 추가하지 않는다.
* 실제 측정하지 않은 성능 수치를 임의로 생성하지 않는다.
* 실제 담당하지 않은 업무를 팀원의 담당 업무로 표현하지 않는다.
* 확인되지 않은 기술적 구현 내용을 사실처럼 작성하지 않는다.
* 일반적인 스마트팩토리 사례를 프로젝트에 실제 적용한 것처럼 확장하지 않는다.
* 정보가 부족한 부분은 임의로 채우지 말고 [확인 필요], [추후 작성] 등으로 표시한다.

### 2) 슬라이드 수는 40장으로 고정하지 않는다
* 발표 내용상 필요한 경우 40장보다 많아지거나 적어져도 된다.
* 한 슬라이드에 내용이 너무 많아지는 경우 기존 지정 내용을 2장으로 분리하는 것은 허용한다.
* 단, 슬라이드 수를 늘리기 위해 새로운 주제나 기능을 임의로 만들어 추가하지 않는다. (기존 내용의 분할은 허용하지만, 새로운 내용의 창작은 금지한다.)

### 3) 초안이라는 점을 명확하게 유지
아직 발표자료 제작 및 일부 기능 구현이 진행 중이므로, 구현 상태가 확정되지 않은 내용에는 다음과 같은 상태 표시를 추가한다.
* [구현 완료] / [구현 중] / [구현 예정] / [검증 중] / [추후 확인]
* 특히 구현 예정인 기능을 이미 구현 완료한 것처럼 표현하지 않는다. 나중에 쉽게 수정할 수 있도록 슬라이드 본문 또는 비고에 명확하게 표시한다.

---

# 2. 프로젝트 전체 메시지
프로젝트의 핵심 메시지는 다음 방향으로 유지한다.
> "AI가 모든 것을 독단적으로 판단하는 시스템이 아니라, AI가 불량 후보를 검출하고 작업자가 이를 검수하여 최종 판정을 내리는 Human-in-the-Loop 기반 스마트 MES"

* 핵심 프로세스:
  PCB 이미지 입력 ➔ YOLO AI 불량 검출 ➔ Bounding Box 및 불량 유형 표시 ➔ 작업자 검수 (승인 / 수정 / 오탐 처리) ➔ 최종 판정 ➔ DB 반영 및 분석
* AI가 최종 판정을 내리는 것처럼 보이지 않도록 흐름을 정확히 통제한다.

---

# 3. 프로젝트 배경 / 수주 시나리오 관련 수정
* 기존 초안의 [슬라이드 1-2: 수주 시나리오 및 발주사 요구사항] 내용은 우선 보류 성격으로 유지한다.
* 해당 내용이 프로젝트의 배경 및 요구사항 설명이라면 현재 초안에서는 크게 확장하지 않는다.
* 단, 배경 설명을 넘어 실제 시스템 기능 또는 기술 구현을 설명하는 내용(실시간 공정 모니터링, WebSocket 기반 상태 전달, AI 불량 검출, 작업자 검수, AI 챗봇, 생산/검사 데이터 연계)이라면 기능/기술 파트에서 사용할 수 있도록 적절히 녹여낸다. 배경 설명과 실제 구현 설명을 혼동하지 않는다.

---

# 4. 시스템 목표 및 핵심 구조

### 핵심 목표
AI 기반 PCB 불량 검출 + 작업자 검수 + 생산/공정 데이터 관리 + 실시간 모니터링을 하나의 MES 흐름으로 통합

### 핵심 축
* 축 ① 실시간 공정 / 생산 관리
  - Dashboard [구현 완료]
  - 생산계획 [구현 완료]
  - 배치 관리 [구현 완료]
  - 라인 센서 상태 모니터링 [구현 완료]
  - AGV 상태전이 시뮬레이션 [구현 완료]
  - WebSocket 기반 실시간 정보 연동 [구현 완료]
* 축 ② AI Vision / PCB 불량 검수
  - YOLO 기반 PCB 불량 검출 및 6종 불량 유형 분류 [구현 완료]
  - Bounding Box 시각화 [구현 완료]
  - 작업자 검수 화면 (승인 / 수정 / 오탐) [구현 완료]
  - 최종 결과 DB 반영 [구현 완료]
  - OpenCV 기반 불량 위치 히트맵 [구현 완료 / 검증 필요]
  - Pareto Chart (불량 패턴 분석) [구현 완료 / 검증 필요]
  - Excel 보고서 내보내기 (openpyxl 사용) [구현 완료]
  - PDF 보고서 내보내기 (reportlab 사용) [구현 완료]
* 축 ③ AI 챗봇 / 부가 기능
  - OpenAI GPT API 기반 챗봇 (기존 로컬 Ollama 모델 테스트 완료 후 본 서비스 전환 적용 중) [구현 중]
  - 생산 및 시스템 매뉴얼 관련 질의응답 기능 [구현 중]

---

# 5. 팀원 역할 표현 수정
* 팀원을 FE 담당 / BE 담당으로 이분법하여 고정해서 표현하지 않는다.
* 실제 개발 방식:
  - [공통 개발]: Frontend와 Backend는 기능 단위로 나누어 유기적으로 협업. 특정 팀원이 Frontend 전체를 담당하거나 Backend 전체를 담당한 구조가 아님. 멘토 제공 기본 DB 구조를 기반으로 프로젝트 기능에 필요한 데이터 구조(테이블)를 팀에서 공동으로 추가/수정하여 연계함.
  - [본인 주요 담당]: AI / YOLO 모델 학습 및 비교 실험, PCB 불량 검출, 데이터 전처리 및 타일링 관련 실험, 모델 평가 및 GT 기반 정량 검증, AI 검수 관련 기능 및 관련 FE/BE 기능 구현.
  - [팀원 주요 담당]: 인프라 및 배포(Docker Compose, CI/CD 배포 자동화 파이프라인), FE/BE 기능 단위 협업.
* 따라서 [FE 담당자 / BE 담당자] 표현은 절대 금지하며, [기능 단위 협업] 및 [AI / 인프라 등 주요 담당 영역] 형태로 표현한다.

---

# 6. DB 관련 표현 수정
* 기존의 "MariaDB 17개 테이블 최적화 설계"라는 표현은 삭제하거나 수정한다.
* DB 전체 구조를 팀이 처음부터 설계하고 최적화한 것이 아니므로 다음과 같이 표현한다.
  > "멘토가 제공한 기본 DB 구조를 기반으로 프로젝트 기능(생산 배치 라이프사이클 관리, 16개 적재 버퍼 슬롯, BOM 기반 부품 소비 이력 등)에 필요한 데이터 구조를 추가 확장하고 API 및 기능과 연계"
* "DB 전체 설계", "DB 최적화 설계" 등의 과도한 표현은 사용하지 않는다.

---

# 7. AI 모델 실험 파트 (비교적 자세하게 구성)
* 본인 담당 영역이므로 AI / YOLO 부분은 다른 파트보다 상대적으로 자세하게 구성해도 된다. 다음 실제 실험 데이터를 장표 구성에 활용한다.

### 1) PCB 불량 6종 클래스 명칭
* `missing_hole` (미싱 홀), `mouse_bite` (마우스 바이트), `open_circuit` (오픈 서킷), `short` (쇼트), `spur` (스퍼), `spurious_copper` (스퓨리어스 카퍼)

### 2) 모델 및 실험 구조
* 비교 모델: YOLOv8 vs YOLO11 (모델 크기 및 구조 비교)
* 학습 전처리 방식: 원본 이미지 기반 학습 vs 타일링 기반 학습 (Tile overlap 실험)
* 추론 방식: 일반 추론(Original) vs 2x2 / 9-tile Tiling 기반 추론 비교
* 임계치 실험: Confidence threshold sweep 실험 진행
* 검증 방식: Pascal VOC XML 기반 Ground Truth(GT) annotation 데이터셋과 매칭하여 정량 평가 (Precision, Recall, F1-score, mAP@0.5, mAP@0.5:0.95 계산)

### 3) 실측 실험 수치 (임의 생성 금지, 아래 데이터만 사용)
* **YOLOv8n Baseline (Original 이미지 추론)**: F1-score `0.8427` / mAP@0.5 `0.7562`
* **YOLOv8n + 2x2 Tiling 추론**: F1-score `0.7741`
* **YOLOv8n + 9-Tile Tiling 추론**: F1-score `0.8870`
* 현재 최종 배포 모델은 정밀 튜닝 및 선정 검증이 진행 중이므로 `[최종 검증 후 확정]` 또는 `[모델 선정 진행 중]`으로 표시한다.

---

# 8. 데이터 누출(Data Leakage) 관련 표현 주의
* 데이터 누출 디버깅 내용을 프로젝트 전체의 핵심 기능처럼 과도하게 묘사하지 않는다.
* 이 내용은 AI 모델 신뢰성 검증 과정의 기술적 트러블슈팅 사례로 아래 흐름에 맞게 표현한다.
  - 흐름: 데이터셋 분할 및 타일링 방식 검토 ➔ train/val split 전 타일링으로 인한 Data Leakage 가능성 확인 ➔ 검증 데이터셋 구성 방식 재검토 ➔ GT 기반 독립 평가셋 분리 설계 ➔ 모델 성능 재검증 및 신뢰성 확보
  - 메시지: "프로젝트의 배경이 아니라 AI 모델 신뢰성 검증 과정의 기술적 해결 사례"

---

# 9. AI 검수 화면의 핵심 프로세스
기존 표현을 다음과 같이 수정하여 AI가 이미 판정을 최종 확정한 것처럼 보이는 것을 방지한다.
* 적용 흐름도:
  YOLO AI 불량 후보 검출 ➔ Bounding Box + 불량 유형 표시 ➔ 작업자 검수 (승인 / 불량 유형 수정 / 오탐 처리 중 선택) ➔ 최종 검수 결과 확정 및 DB 저장
* 핵심 메시지: "AI는 검출을 보조하고, 최종 품질 판정은 작업자가 수행한다."

---

# 10. 구현 상태 표시 가이드 (최종 확인 상태)
장표 제작 시 아래의 실제 구현 상태를 각 슬라이드 본문 또는 비고에 명확히 명시한다. 젠스파크가 임의로 상태를 판단하거나 지어내지 않게 제어한다.
* Excel 보고서 내보내기: `[구현 완료]`
* PDF 보고서 내보내기: `[구현 완료]`
* OpenAI 챗봇 서비스: `[구현 중]`
* OpenCV 불량 위치 히트맵: `[구현 완료 / 검증 필요]`
* Pareto Chart 통계 분석: `[구현 완료 / 검증 필요]`
* 실시간 대시보드 및 WebSocket 통신: `[구현 완료]`
* 상태가 아직 불확실하다고 판단될 시: `[상태 확인 필요]` 로 남긴다.

---

# 11. 기대효과 수치에 대한 주의
기존 초안에 작성되었던 아래 수치들은 실측 데이터 수집 및 검증 단계이므로, 기정사실화하여 성과로 표현하지 말고 다음과 같이 처리한다.
* 대상 수치: 대응 시간 40% 단축, 육안 검수 시간 50% 절감, API 50ms, WebSocket 10ms, YOLO 80ms 등
* 가이드: 실측 데이터가 완전히 확인되기 전까지 장표 상에 수치를 고정하지 않고 **`[실측 데이터 필요]`** 또는 **`[검증 후 수치 확정 예정]`**으로 표시하여 초안으로서의 객관성을 유지한다.

---

# 12. 화면 Walkthrough 구성
화면 중심 슬라이드는 기존 설계 구조를 최대한 유지하되, 실제 화면이 아직 개발되지 않은 기능은 화면이 구현 완료되어 있는 것처럼 시각자료를 거짓 생성하지 않는다.
* Dashboard: 생산량, 검사량, 불량률, 센서 상태, 실시간 알람
* AGV / 공정: AGV 로봇 상태, 생산 라인 가동 상태, WebSocket 실시간 통신
* 검사 화면: PCB 이미지, Bounding Box 및 6종 불량 유형 표시
* Defect Review (검수): AI 검출 결과 표시, 작업자 검수 선택 도구 (승인/수정/오탐)
* Analysis (분석): 불량 유형 통계, PCB 모델별 불량, 히트맵 위치 분석, 검수 매트릭스
* Report (보고서): Excel 다운로드, PDF 출력

---

# 13. 슬라이드 분할 및 디자인 규칙
* [슬라이드 분할]: 한 페이지에 내용이 너무 많으면 기존 지정 내용을 2페이지로 자유롭게 분리할 수 있다. (예: YOLO 모델 학습 및 비교 실험 ➔ 3-4-1. 데이터 전처리 및 학습 방식 / 3-4-2. 모델 성능 비교 및 검증). 단, 페이지를 늘리기 위해 새로운 기능이나 기술을 임의로 지어내지 않는다.
* [디자인 Theme]: Sleek Dark Mode (짙은 Navy / Charcoal 배경, Mint / Blue 포인트 컬러). 스마트팩토리, PCB Vision 검사의 첨단 테크니컬 느낌 강조.
* [시각화 우선]: 긴 텍스트 대신 Process Flow, Matrix, 비교표, KPI 카드, 실제 UI 스크린샷 등을 사용한다. 특히 AI 모델 실험 부분은 수치 비교가 표나 그래프로 잘 보이도록 설계한다.

---

# 14. 최종 발표 스토리 흐름 (순서 준수)
프로젝트 배경 ➔ 스마트 MES 도입 필요성 ➔ 전체 시스템 아키텍처 ➔ 생산 / 공정 / AGV 모니터링 ➔ AI Vision PCB 검사 (YOLO 모델 학습 및 검증) ➔ AI 검출 및 작업자 검수 (Human-in-the-Loop) ➔ 분석 / 리포트 ➔ OpenAI 챗봇 ➔ 개발 과정에서의 극복 사례(Data Leakage) ➔ 기대효과 (검증 필요) ➔ 향후 발전 방향 ➔ 통합 Demo
