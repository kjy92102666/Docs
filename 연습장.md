
지금 혼란의 원인은 “센서가 PCB 불량을 발생시킨 원인인지”와 “센서가 검사 환경을 측정한 것인지”가 구분되지 않았기 때문입니다.

현재 프로젝트 범위에서는 센서값을 PCB 정상·불량률에 바로 반영하면 논리적으로 맞지 않을 가능성이 큽니다.

## 먼저 구분해야 하는 센서의 위치

### 1. PCB 생산라인 센서

PCB가 실제로 생산될 당시의 센서입니다.

```
PCB 생산 당시
고온·고진동·높은 먼지
        ↓
PCB 제조 품질에 영향
        ↓
불량 발생 가능성 증가
```

이 경우에는 센서가 PCB 불량 발생과 연관될 수 있습니다.

하지만 현재 프로젝트는 PCB를 직접 생산하지 않고, 이미 생산되어 입고된 PCB를 검사합니다. 따라서 생산 당시 센서 데이터가 별도로 전달되지 않는다면 현재 공장의 센서값으로 PCB가 왜 불량이 됐는지 설명할 수 없습니다.

### 2. PCB 검사라인 센서

입고된 PCB를 비전 검사하는 현재 라인의 센서입니다.

```
이미 생산된 PCB
        ↓
검사라인 진입
        ↓
카메라 촬영·YOLO 검사
```

이 센서는 이미 발생한 PCB 불량의 원인이 아닙니다. 대신 검사 품질에 영향을 줄 수 있습니다.

예:

- 진동이 크면 카메라 이미지가 흔들림
- 먼지가 많으면 PCB 표면 또는 렌즈가 가려짐
- 온도 이상으로 검사 장비 성능이 불안정해짐
- 조명이나 환경 이상으로 YOLO 신뢰도가 낮아짐

따라서 검사라인 센서는 실제 PCB 불량률보다 검사 신뢰성과 연관 짓는 것이 맞습니다.

## 현재 프로젝트에 맞는 해석

현재 `sensor_data`가 검사 라인의 `line_id`와 연결되어 있다면 다음 구조가 가장 자연스럽습니다.

```
PCB 실제 상태
→ 이미 입고 전에 결정됨

YOLO
→ PCB 이미지를 분석해 정상/불량 예측

검사라인 센서
→ 당시 검사 환경이 안정적이었는지 판단
```

최종 결과는 분리해야 합니다.

```
비전 결과
정상/불량, 불량 유형, YOLO 신뢰도

센서 결과
검사 환경 정상/경고/위험

업무 결과
정상 후보/불량 후보/재검사 필요
```

예를 들면:

|YOLO 결과|센서 환경|처리|
|---|---|---|
|정상|정상|정상 후보|
|불량|정상|불량 확인 대상|
|정상|위험|환경 문제 가능성, 재검사|
|불량|위험|불량 후보지만 환경 안정 후 재검사 권장|

센서값을 이용해 YOLO가 정상으로 판정한 PCB를 바로 불량으로 바꾸는 것은 권장하지 않습니다.

## “센서 영향을 받아 정상률 90%”의 의미

이 표현은 현재 상태에서는 정의가 불명확합니다.

```
센서 영향 정상률 90%
→ 무엇을 기준으로 한 90%인지 알 수 없음
```

다음 중 어느 의미인지 구분해야 합니다.

### 의미 A: 센서 이상 시간대의 PCB 정상률

```
센서 이상 시간대 검사 PCB 100개
최종 정상 PCB 90개
→ 센서 이상 구간 정상률 90%
→ 불량률 10%
```

이 계산 자체는 가능합니다.

하지만 센서 때문에 10개가 불량해졌다고 단정할 수는 없습니다. 단순히 같은 시간에 센서 이상과 불량이 함께 관찰됐다는 상관관계입니다.

### 의미 B: 센서 상태를 반영한 AI 정상 확률

```
YOLO 정상 신뢰도: 95%
센서 환경 위험도: 높음
최종 정상 신뢰도: 90%
```

이 방식은 센서 결합 모델이나 명확한 계산 공식이 있어야 합니다. 현재는 그런 모델이나 검증된 가중치가 없으므로 임의로 `95% → 90%`처럼 낮추면 과학적 근거가 부족합니다.

발표용으로 임의 가중치를 넣을 수는 있지만 반드시 “규칙 기반 시뮬레이션 점수”라고 표시해야 합니다.

## 센서와 PCB는 어떻게 연결하는가

센서 테이블에 PCB 외래키를 직접 넣을 필요는 없습니다. 라인과 검사 시간을 이용해 연결할 수 있습니다.

```
PCB-00001
검사 라인: LINE-01
검사 시각: 10:00:05
        ↓
LINE-01의 10:00:04~10:00:06 센서 데이터
```

관계는 다음과 같습니다.

```
PCB
→ 배치
→ 검사 라인

PCB 검사 결과
→ 검사 시각

sensor_data
→ line_id
→ reading_at
```

결합 조건:

```
배치의 line_id = sensor_data.line_id
PCB inspected_at과 가까운 sensor_data.reading_at
```

조금 더 안정적으로 구현하려면 `inspection_run`을 둡니다.

```
inspection_run
--------------
run_id
batch_id
line_id
started_at
ended_at
```

그런 다음 검사 실행 시간 범위에 포함된 센서를 연결합니다.

```
sensor_data.line_id = inspection_run.line_id
sensor_data.reading_at BETWEEN started_at AND ended_at
```

PCB별로는 해당 PCB 검사 시점의 최근 센서값이나 전후 몇 초 평균을 사용할 수 있습니다.

## 추천 센서 활용 방식

현재 프로젝트에서는 센서를 PCB 불량 원인으로 사용하기보다 검사 환경 품질로 사용하는 것을 추천합니다.

### 비전 불량률

```
YOLO 불량 후보 수 / 전체 검사 PCB 수
```

### 최종 불량률

```
작업자가 확정한 불량 PCB 수 / 전체 검사 PCB 수
```

### 센서 이상 구간 불량률

```
센서 이상 시간대에 검사된 최종 불량 PCB 수
/
센서 이상 시간대에 검사된 전체 PCB 수
```

### 센서 정상 구간 불량률

```
센서 정상 시간대에 검사된 최종 불량 PCB 수
/
센서 정상 시간대에 검사된 전체 PCB 수
```

두 값을 비교하면 상관관계를 분석할 수 있습니다.

```
센서 정상 구간 불량률: 3%
센서 이상 구간 불량률: 8%
```

이 경우 올바른 설명은 다음과 같습니다.

> 센서 이상 구간에서 검사된 PCB의 불량률이 더 높게 관찰됐다.

다음처럼 말하면 안 됩니다.

> 센서 이상 때문에 불량률이 5% 증가했다.

원인 관계를 주장하려면 생산 당시 센서 데이터, 충분한 표본, 다른 조건 통제와 통계 분석이 필요합니다.

## 생산 당시 센서를 사용하고 싶다면

센서를 PCB 불량 발생 원인과 연결하려면 현재 검사라인 센서가 아니라 PCB 생산 당시의 데이터가 필요합니다.

최소한 다음 정보가 함께 들어와야 합니다.

```
PCB ID
생산 배치 ID
생산 라인
생산 시작·완료 시간
해당 생산 시간대 센서 데이터
```

그때는 다음 분석이 가능합니다.

```
생산라인 센서 이상
→ 해당 시간대 생산 PCB 식별
→ 입고 후 YOLO 검사 결과 연결
→ 센서 이상과 불량 유형의 상관관계 분석
```

하지만 이는 현재 “입고 PCB 비전검사” 범위를 넘어 생산 이력 연동까지 확장하는 작업입니다.

# 현재 소스 수정을 진행해도 되는가

검사계획 등록부터 배치 준비 확인까지는 센서 문제가 확정되지 않아도 진행할 수 있습니다.

이 단계들의 책임은 센서와 독립적입니다.

```
검사계획 등록
→ 검사 날짜·수량·라인 결정

배치 등록
→ 입고 PCB 식별번호 생성

이미지 연결
→ PCB별 검사 이미지 고정

배치 준비 확인
→ PCB 수량과 이미지 파일 존재 확인
```

배치 준비 조건에 센서를 포함하지 않으면 됩니다.

```
배치 준비 완료 조건
- 배치 PCB 수가 계획 수량과 일치
- 모든 PCB에 검사 이미지 연결
- 연결된 이미지 파일 조회 가능
```

센서는 검사 시작 이후에만 필요합니다.

따라서 지금 안전하게 진행할 수 있는 범위는 다음과 같습니다.

- 검사계획 등록 유지
- 입고 배치 등록
- PCB ID 생성
- PCB별 이미지 고정 연결
- 이미지 재사용 허용
- 배치 준비 상태 계산
- 이미지 조회 API
- 생산 관련 표현을 입고·검사 표현으로 정리

지금 보류해야 하는 범위:

- 검사 시작 로직
- YOLO 결과와 센서 결합 공식
- 센서 기반 정상률 계산
- 센서 이상 시 재검사 정책
- 검사 결과·센서 데이터 연결 테이블
- 작업자 최종 판정 이후 재고 전이

결론적으로 배치 준비 단계 수정은 진행해도 문제가 없습니다. 다만 센서를 `inspection_ready` 조건이나 PCB의 실제 정상·불량 정답 생성에 포함하지 말아야 합니다. 현재 프로젝트에서는 센서를 검사 환경 데이터로 정의하고, 검사 단계 설계 시 `line_id + 검사 시간`으로 PCB 검사 결과와 연결하는 것이 가장 일관된 방향입니다.