# 프로젝트 구조 개선 및 리팩토링 제안

## 1. 공정상태(Process Status) 기능 구조

### 핵심 파일

현재 공정상태 기능의 핵심 파일은 다음 3개이다.

```text
agvSimulation.js
AgvLineControl.jsx
AgvLineControl.css
```

---

## 권장 폴더 구조

```text
frontend/src/
├── domains/
│   └── agv/
│       └── agvSimulation.js
│
└── features/
    └── process-status/
        ├── ProcessStatus.jsx
        └── components/
            ├── AgvLineControl.jsx
            └── AgvLineControl.css
```

---

## 역할 분리

| 파일 | 역할 |
|------|------|
| agvSimulation.js | AGV 및 라인 상태 전이 로직 |
| AgvLineControl.jsx | 공정상태 화면 UI |
| AgvLineControl.css | 공정상태 UI 스타일 |
| ProcessStatus.jsx | 공정상태 Feature 진입점 |

---

### 왜 domains/agv 인가?

현재는 Process Status 화면에서만 사용하지만,

향후

- Dashboard
- Backend 연동
- WebSocket
- AGV 제어

등에서도 재사용될 가능성이 높기 때문이다.

즉,

```text
Domain Logic
```

로 관리하는 것이 적합하다.

---

# 테스트 구조

현재

```text
agvControl.test.js
```

안에는

- 시뮬레이션 테스트
- UI 테스트

가 섞여 있다.

다음처럼 분리하는 것이 좋다.

```text
frontend/test/
├── domains/
│   └── agv/
│       ├── agvSimulation.test.js
│       └── agvZoneLayout.test.js
│
└── features/
    └── process-status/
        └── AgvLineControl.test.js
```

> 파일 이동과 테스트 리팩토링은 한 번에 하지 말고 단계적으로 진행하는 것이 안전하다.

---

# 2. AgvRealtimeOverview 위치

현재 위치

```text
frontend/src/features/dashboard/components/
└── AgvRealtimeOverview.jsx
```

현재 위치가 맞다.

이 컴포넌트는

Dashboard용이다.

---

## 이름 변경

현재는

```text
AgvRealtimeOverview
```

사용

↓

대시보드 요구사항이 확정되면

```text
AgvStatusSummary
```

등으로 변경해도 된다.

---

## CSS 문제

현재

```text
Dashboard
```

가

```text
AgvLineControl.css
```

를 사용하고 있다.

이는 Feature 간 의존성이 생긴다.

### 향후 개선 방향

- Dashboard 전용 CSS 생성
- 공통 AGV 스타일(shared) 분리
- Dashboard 확정 후 CSS 재설계

현재는

파일 위치만 유지하고

스타일 분리는 나중에 진행해도 된다.

---

# 3. .ai 폴더

여기서 AI는

YOLO

Chatbot

추론 코드

를 의미하는 것이 아니다.

코딩 에이전트가 프로젝트를 이해하기 위한 문서이다.

---

## 권장 구조

```text
.ai/
├── PROJECT.md
└── STATUS.md
```

---

## RULES.md는?

이미

```text
AGENTS.md
```

가 존재한다.

따라서

```text
.ai/RULES.md
```

를 따로 작성하면

규칙 충돌

중복

최신 문서 혼란

이 발생할 수 있다.

---

## 권장

에이전트 규칙

↓

AGENTS.md

하나만 사용

---

# 실제 AI 기능 위치

YOLO

추론

학습

챗봇

등은

.ai에 두지 않는다.

---

## 권장 구조

```text
backend/
├── vision/
│   ├── inference/
│   ├── training/
│   ├── models/
│   └── README.md
│
└── app/
    └── features/
        ├── inspection/
        ├── review/
        └── chatbot/
            └── prompts.py
```

---

## AI 전체 설계 문서

다음 위치를 추천

```text
docs/architecture/
└── ai-components.md
```

기록 내용

- YOLO 입력/출력
- 추론 흐름
- 자동 판정
- 사용자 검토
- 모델 버전 관리
- Prompt 관리
- 개인정보 처리

---

## AI 관련 역할

| 위치 | 역할 |
|------|------|
| AGENTS.md | 코딩 에이전트 규칙 |
| docs/architecture | AI 설계 문서 |
| backend/vision | AI 실행 코드 |

---

# 4. DB 구조 변경

현재

```text
backend/db/
├── schema.sql
├── pcb_seed.sql
├── bom_seed.sql
└── migrations/
```

---

## 권장 구조

```text
backend/db/
├── schema.sql
├── migrations/
│   └── YYYYMMDD_description.sql
└── seeds/
    ├── pcb_seed.sql
    └── bom_seed.sql
```

---

## 역할

| 폴더 | 역할 |
|------|------|
| schema.sql | 신규 DB 생성 기준 |
| migrations | 기존 DB 변경 |
| seeds | 개발용 초기 데이터 |

---

# 함께 수정해야 하는 파일

Seed 위치를 변경하면

다음도 함께 수정해야 한다.

```text
backend/app/main.py

backend/scripts/generate_bom_seed.py

backend/db/migrations/*.sql

README.md

설계 문서
```

특히

```text
generate_bom_seed.py
```

는

출력 경로가

```text
backend/db/bom_seed.sql
```

로 고정되어 있으므로

수정하지 않으면

기존 위치에 계속 생성된다.

---

# Seed 이동 체크리스트

- [ ] backend/db/seeds 생성
- [ ] pcb_seed.sql 이동
- [ ] bom_seed.sql 이동
- [ ] generate_bom_seed.py 출력 경로 수정
- [ ] main.py 안내 수정
- [ ] Migration SQL 주석 수정
- [ ] README 수정
- [ ] DB 적재 테스트

---

## schema.sql

현재 위치 유지

```text
backend/db/schema.sql
```

추천

별도

```text
schema/
```

폴더는 현재 규모에서는 불필요.

---

# Dump 파일

다음 파일은

Seed가 아니다.

```text
pcb_mes_dump.sql

backup_before_20260728.sql
```

따라서

```text
seeds/
```

에 두면 안 된다.

---

## 권장

```text
archive/
```

또는

백업 정책으로 별도 관리.

---

# 최종 권장 사항

## Frontend

```text
domains
    ↓
공통 비즈니스 로직

features
    ↓
화면 기능
```

---

## Backend

```text
vision
    ↓
AI 실행 코드

features
    ↓
비즈니스 기능
```

---

## AI

```text
AGENTS.md
        ↓
코딩 규칙

docs/architecture
        ↓
AI 설계

backend/vision
        ↓
실행 코드
```

---

## Database

```text
schema.sql
        ↓
전체 생성

migrations/
        ↓
버전 관리

seeds/
        ↓
개발 데이터

archive/
        ↓
백업
```

---

# 핵심 결론

✅ 공정상태 기능 분리 방향은 적절하다.

✅ AGV 시뮬레이션은 Domain으로 분리하는 것이 좋다.

✅ Dashboard는 현재 위치를 유지한다.

✅ `.ai/RULES.md`보다 `AGENTS.md` 단일 관리가 바람직하다.

✅ 실제 AI 설계는 `docs/architecture`에서 관리한다.

✅ AI 실행 코드는 `backend/vision`에 위치한다.

✅ DB는 `schema`, `migrations`, `seeds` 역할을 명확히 분리한다.

✅ Seed 이동 시 관련 스크립트와 문서 경로까지 함께 수정해야 한다.







  ### PCB 이미지 데이터셋 관리                                                                                    
                                                                                                                  
  PCB 원본 이미지는 저장소에 포함하지 않으며 현재 Google Drive에서 관리합니다.                                    
  각 개발자는 공유된 Google Drive 데이터셋을 로컬 환경에 연결한 후,                                               
  `backend/.env`의 `PCB_DATASET_PATH`에 실제 데이터셋 경로를 설정합니다.                                          
                                                                                                                  
  ```env                                                                                                          
  PCB_DATASET_PATH=C:/path/to/PCB_DATASET


![[Pasted image 20260730124617.png]]
