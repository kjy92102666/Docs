# CI/CD · AWS 전환 작업 계획 (2026-08-11 ~)

> **발표일**: 2026-08-22 (토)
> **선행 문서**: `2026-08-09~10_사전테스트_최종결과.md`

---

## 1. 보안 · 비용 가드레일 (착수 전 필수 확정)

이전 단계에서 "DB를 public으로 열어 비용이 급증하는 상황을 어떻게 방지할 것인가"를 검토했다.
**핵심 원칙: DB는 처음부터 인터넷에 노출될 경로 자체를 만들지 않는다.**

### 1-1. DB는 도커 내부망으로만 통신

```yaml
# docker-compose.yml
mariadb:
  image: mariadb:11
  # ports: 항목을 절대 쓰지 않음 — 호스트에 3306을 매핑하지 않음
  networks:
    - internal
  environment:
    MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}   # .env로만 관리, 커밋 금지

backend:
  networks:
    - internal
  # backend는 mariadb:3306 으로 내부 네트워크 접속
```

FastAPI 컨테이너는 도커 내부 네트워크(`mariadb:3306`)로 접속하며, 이는 Lightsail 방화벽이나 외부 인터넷과 무관하다.
**"실수로 DB 포트를 열 수 있는 설정 지점"을 아예 없애는 것이 목표다.**

### 1-2. Lightsail 방화벽 — 허용 최소 원칙

| 포트 | 용도 | 소스 |
|---|---|---|
| 22 | SSH | 가능하면 팀원 고정 IP로 제한, 어려우면 전체 허용 유지 |
| 80 | HTTP | 전체 허용 |
| 443 | HTTPS (적용 시) | 전체 허용 |
| 3306 | DB | **등록하지 않음** |

> 기존 `pcb-mes-db-dev` 인스턴스에 `MySQL/Aurora 3306` 규칙이 특정 IP(`58.227.90.78/32`)로 제한되어 있음을 확인함.
> 이 방식(단일 IP 제한)은 로컬 DB 툴 접속 등에 필요할 때만 유지하고, **`0.0.0.0/0`으로 변경하지 않는다.**

### 1-3. 서버 내부 이중 방어 (ufw)

Lightsail 콘솔 방화벽 설정과 별개로, 서버 안에서도 기본 거부 정책을 적용한다.

```bash
sudo ufw default deny incoming
sudo ufw allow 22
sudo ufw allow 80
sudo ufw allow 443
sudo ufw enable
sudo ufw status
```

콘솔 설정이 실수로 변경되어도 서버 내부에서 한 번 더 차단하는 이중 구조.

### 1-4. 비용 급증 경로 차단

비용이 튀는 전형적 경로는 "DB 포트가 뚫려 봇의 스캔·무차별 대입 공격이 지속되며 트래픽·연산 자원을 소모하는 경우"다.
1-1~1-3으로 이 경로 자체를 차단하며, 추가로:

- **AWS Budgets 알림** ($30/$60/$80) — 기존 설정 유지, 이상 시 조기 감지
- DB 비밀번호는 강력하게 생성, `.env`로만 관리 (`.gitignore` 확인)
- 연습으로 열어둔 포트(예: 8000)는 실사용 전 정리
- 정기적으로 Lightsail 네트워킹 탭에서 열린 규칙 재검토

---

## 2. 트랙 분리 (재고관리 병행 개발 고려)

| 트랙 | 담당 | 스키마 영향 | 범위 |
|---|---|---|---|
| A · 인프라 | 담당자 A | 없음 | Docker, CI/CD, 서버, 배포 |
| B · 기능 | 담당자 B | 있음 | 재고관리, OpenAI 전환, 데이터 |

**근거**: 백엔드 테스트 282개가 실제 DB에 접속하지 않으므로, Dockerfile·CI·서버 구축은 스키마 변경과 무관하게 진행 가능.

---

## 3. 아키텍처

```
사용자 브라우저
      │
      ▼
Lightsail 4GB 인스턴스 (고정 IP)
  └─ Docker Compose
       ├─ nginx    : React 정적 서빙 + /api, /ws 프록시 (동일 오리진 → CORS 문제 소멸)
       ├─ backend  : FastAPI + YOLO   (도커 내부망으로만 mariadb 접속)
       └─ mariadb  : DB               (호스트에 포트 노출 없음)
  └─ 볼륨 마운트 : PCB 이미지, best.pt
```

**예상 비용**: 월 $20~25 (크레딧 $100으로 4개월 이상)

### Codex 제안(ECS+ALB+RDS+SQS+private subnet)과의 차이

| 항목 | Codex 제안 | 채택안 | 사유 |
|---|---|---|---|
| 프론트 | S3+CloudFront | nginx 동일 서버 | CORS 소멸, 구성 단순화 |
| 백엔드 | ECS Fargate+ALB | 단일 서버 Docker | 태스크 1개면 ECS 이점 없음 |
| DB | RDS 별도 | 컨테이너(내부망) | 월 $20 절감 |
| 네트워크 | private subnet+NAT | public+보안그룹+ufw | NAT $32/월 회피 |
| 검사 큐 | SQS+Worker | 현행 유지 | 단일 서버에선 무의미 |

Codex 제안 비용 추산 월 $91~120 → 무료 플랜 크레딧이 한 달 내 소진되는 리스크 있어 발표 범위에서 제외.
목표 아키텍처로서 발표 자료 "향후 계획" 섹션에 활용.

---

## 4. 일정표

| 날짜 | A · 인프라 | B · 기능 | 마일스톤 |
|---|---|---|---|
| **8/11 (화)** | `feature/ci-setup` dev 병합(트리거 추가), Dockerfile+compose 작성 | DB 리셋 스크립트, 환경변수화 | |
| **8/12 (수)** | **로컬 전체 구동 검증** | 재고관리 개발 | ⚠️ 판단 시점 |
| **8/13 (목)** | Lightsail 4GB 생성, 방화벽(1장 원칙 적용), 첫 배포 | OpenAI Provider 전환 | |
| **8/14 (금)** | 배포 문제 해결(버퍼) | 재고관리 마무리 | |
| **8/15 (토)** | health check, CD 워크플로우(수동 트리거) | 재고관리 완료 | 🔒 스키마 동결 |
| **8/16 (일)** | 스키마 반영 재배포, YOLO fixture+Release 업로드 | 최종 데이터 적재 | |
| **8/17 (월)** | YOLO 배치 실측, 사양 조정 | 전 기능 점검 | |
| **8/18 (화)** | 성능 튜닝, DB 백업 cron | 데모 시나리오 확정 | |
| **8/19 (수)** | **전체 리허설** | 백업 영상 녹화 | |
| **8/20 (목)** | 발표 자료(인프라) | 발표 자료 | 🔒 코드 동결 |
| **8/21 (금)** | 예비일 | 리허설 2회차 | |
| **8/22 (토)** | **발표** | | 🎯 |

---

## 5. Phase별 상세

### Phase 1 · 코드 준비 (8/11~8/12)

- CI에 dev/main 트리거 추가 → PR → 병합
- 환경변수화: `main.py` CORS, `database.py` 커넥션 풀, `dataset.py` 경로, 프론트 `VITE_` 변수
  - `test_cors_config.py` 동반 수정 필요
- Health check: `/health/live`, `/health/ready`(DB 연결+YOLO 모델 로드 확인)
- Dockerfile: **torch 포함 베이스 이미지 사용** (8/10 확인된 OOM 리스크 사전 차단)
  - `.gitattributes` 추가 (CRLF→LF, Linux 실행 오류 방지)
- docker-compose.yml: nginx/backend/mariadb, **DB는 1장 원칙에 따라 포트 미노출**
- DB 리셋 스크립트: drop→schema.sql 재생성→더미데이터, 멱등성 보장

> **완료 조건**: 로컬에서 `docker compose up` 한 번으로 전체 구동

### Phase 2 · 서버 구축 (8/13~8/14)

- Lightsail 4GB 생성, 고정 IP 할당
- **방화벽: 22/80/443만 개방, 3306 등록 안 함** (1장 참조)
- ufw 이중 설정
- Docker+Compose 설치 → clone → `.env` 작성(강력한 DB 비밀번호) → `docker compose up -d`

> **완료 조건**: 외부 브라우저에서 `http://고정IP/health/ready` 응답

### Phase 3 · CD 구축 (8/15~8/16)

```
workflow_dispatch (수동 실행 버튼)
  → SSH 접속 → git pull → docker compose up -d --build
  → curl /health/ready 검증
```

자동 트리거는 발표 후로 보류 (리허설 중 의도치 않은 재배포 방지).

YOLO 스모크 테스트: fixture 이미지 커밋, `best.pt`는 GitHub Release에서 CI가 다운로드.

### Phase 4 · 검증 및 안정화 (8/17~8/19)

- YOLO 배치 실측(200개 소요 시간, 메모리 피크) → 부족 시 8GB 증설
- DB 덤프 cron 설정
- 전체 리허설 + 백업 영상 녹화

---

## 6. 동결 규칙

| 시점 | 대상 | 이후 허용 범위 |
|---|---|---|
| 8/15 | DB 스키마 | 컬럼 추가 등 가산적 변경만 |
| 8/20 | 전체 코드 | 치명적 버그 수정만 |

---

## 7. 중단 기준 (Go/No-Go)

| 기한 | 조건 | 대응 |
|---|---|---|
| 8/12 | 로컬 docker compose 구동 실패 | AWS 배포 포기, 로컬 데모 전환 |
| 8/13 | OpenAI Provider 전환 실패 | 챗봇 데모 제외 |
| 8/15 | 재고관리 미완 | 미완 상태로 동결 |
| 8/16 | CD 워크플로우 미완 | CD 포기, 수동 배포 유지 |
| 8/17 | 서버 YOLO 추론 실패 | 8GB 증설 → 실패 시 사전 계산 결과로 데모 |

**우선순위**: 배포 > 챗봇 > CI/CD

---

## 8. 배포 런북 초안 (8/10 연습 기반)

Phase 2 진행 시 아래 체크리스트를 그대로 활용한다.

```
[ ] Lightsail 인스턴스 생성, 고정 IP 할당
[ ] 방화벽: 22/80/443만 등록 (3306 등록 금지)
[ ] ufw default deny incoming 설정
[ ] Docker 설치: sudo apt install -y docker.io
[ ] sudo usermod -aG docker $USER → 재로그인 필수
[ ] docker run hello-world 로 권한 확인
[ ] git clone → .env 작성 (DB 비밀번호 강력하게, .gitignore 확인)
[ ] docker compose up -d --build
[ ] 서버 내부에서 curl로 1차 확인
[ ] 외부 브라우저로 2차 확인
[ ] 방화벽 규칙에 소스 IP 반드시 입력 (누락 시 저장 안 됨)
[ ] SSH 배포 키 생성 → 서버 등록 → 저장소 Settings > Secrets and variables > Actions 에 등록
```

---

## 9. 백업 플랜

- 로컬 노트북에서 구동되는 동일 환경 (`docker compose`)
- 정상 동작 화면 녹화 영상 (8/19 촬영)

---

## 10. 발표 산출물 (8/18~19 작성)

| 산출물 | 재료 |
|---|---|
| CI/CD 파이프라인 다이어그램 | 8/9~10 검증 흐름 |
| CI 실행 스크린샷 | 확보 완료 |
| 테스트 현황 | backend 282 / frontend 474, 개선 사례(8→474) |
| 보안·비용 가드레일 설명 | 1장 내용 |
| 목표 아키텍처 | Codex 분석 재활용 |
| 배포 런북 | 8장 |

---

## 11. 발표 이후 로드맵 (참고)

1. 이미지·모델 S3 이전
2. CD 자동 트리거 전환
3. CI required check 승격
4. DB 관리형 분리 (PITR 백업)
5. HTTPS + 도메인
6. DR 체계 (RTO 10분)
7. 상태 외부화 후 HA 전환

---

## 부록 · 기준선

| 항목 | 값 |
|---|---|
| Backend 테스트 | 282 passed, 2 skipped |
| Frontend 테스트 | 474 passed, 0 failed |
| CI 소요 | 1분 56초 |
| Python / Node | 3.11 / 20 |
| 512MB 인스턴스 OOM 확인 | pip install 단계에서 재현 (스왑으로 해결) |
