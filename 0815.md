## 📋 일일 작업 및 진행 상황 보고 (8/15)

### ✅ 오늘 완료한 작업

- **챗봇 기능 서버 배포 완료**
    - `feature/chatbot-openai` → `dev` → `main` 순차 병합, AWS 배포까지 완료
    - `docker-compose.yml`의 backend `environment:`에 `CHATBOT_PROVIDER`/`OPENAI_API_KEY`/`OPENAI_MODEL` 배선 누락 발견·수정 (값을 `.env`에 넣어도 컨테이너에 전달 안 되던 문제)
    - 서버 `.env`에 실제 API 키 반영, `Run workflow`(main)로 재배포
    - 최종 검증: `curl POST /api/chatbot/messages` → `"provider":"openai"` 정상 확인
- **배포 파이프라인 구조 재정비**
    - 원래 설계(main=운영/AWS, dev=개발 통합) 재확인, `deploy.yml`을 `dev` 기준에서 **`main` 기준**으로 전환
    - 헬스체크 타이밍 버그 수정: `sleep 3` 후 단 1회만 curl 시도하던 방식이라 backend가 늦게 뜨면 502로 가짜 실패 발생 → 3초 간격 최대 20회(약 60초) 재시도로 변경, 실제 배포에서 검증 완료
- **DB 백업 방법 확보**

bash

```bash
  docker compose exec mariadb mariadb-dump -h 127.0.0.1 -u root -p pcb_mes > backup_$(date +%Y%m%d).sql
```

수동 백업 정상 동작 확인 (23KB, 정상 SQL 덤프 형식). 자동화(cron)는 8/18 예정대로 보류

- **스키마 동결 — ERD/테이블 정의서 v9 → v10**
    - v9(7/31) 이후 미문서화된 스키마 변경사항 전수 조사 (git log 2026-08-01~08-10 기준)
    - 신규 테이블 2개(`post_inspection_buffer_slot`, `batch_material_consumption`) 및 `production_batch` 물류 컬럼 8개, `inspection_result` 다중 검출 컬럼 5개, `defect_review.corrected_defect_type` — 전부 백엔드+프론트 완전 연동되어 실사용 중 확인
    - `PCB_테이블_정의서_v10.docx` 작성 완료 (17개 테이블 전체)
    - v9 "검토 필요 사항" 8개 항목 재확인 → 대부분 현재 방식 유지로 확정, 발표 범위 내 막힌 핵심 미결 사항 없음
    - 알려진 이슈 재확인: `inspection_ready` 계산 로직의 `line_id` 검증 누락 (v9부터 이어짐, 미해결)
- **PCB 원본 이미지 700장 서버 업로드 및 시딩**
    - 이미지 접근 방식 확정: Google Drive API 아님, **로컬 파일시스템 방식**(`PCB_DATASET_PATH`)
    - `images`(693장) + `PCB_USED`(10장) = 703장(약 998MB) scp 전송 완료
    - `backend/db/pcb_seed.sql` 로드 → `original_pcb` 703건 확인 완료
- **배치/검사 시딩 API 스펙 조사 완료** (실행은 내일로 이월)
    - `POST /api/plans/{plan_id}/batch` (배치 생성)
    - `POST /api/batches/{batch_id}/line-transfer/complete` (라인 이송 완료)
    - `POST /api/batches/{batch_id}/inspect` (시뮬레이션 검사, YOLO 모델 없이도 작동 확인)
- **팀원 PR CI 실패 2건 해결**
    - `openpyxl` 모듈 누락 → `requirements-base.txt`에 추가
    - CI 환경에 DB 없어 발생한 실 DB 의존 테스트 실패 → `@pytest.mark.skipif(os.getenv("CI")=="true")`로 임시 스킵 (정식 해결은 8/18 이후 MariaDB 서비스 컨테이너 추가 예정)

---

### 🏡 내일 이어서 진행할 작업 (To-Do)

#### 1. 데이터 시딩 실행 (조사 완료, 실행만 남음)

bash

```bash
# 1) 현재 plan 목록 확인
curl.exe -sS "http://3.38.222.241/api/plans"

# 2) plan_id별로 반복 (예: plan_id=1)
curl.exe -sS -X POST "http://3.38.222.241/api/plans/1/batch"
# 응답에서 batch_id, current_transfer_id 확인

curl.exe -sS -X POST "http://3.38.222.241/api/batches/{batch_id}/line-transfer/complete" ^
  -H "Content-Type: application/json" ^
  -d "{\"transfer_id\":\"{응답의 transfer_id}\"}"

curl.exe -sS -X POST "http://3.38.222.241/api/batches/{batch_id}/inspect"

# 3) 결과 확인
curl.exe -sS "http://3.38.222.241/api/batches/{batch_id}/inspections"
```

- 시뮬레이션 API(`/inspect`)는 YOLO 모델 없이도 작동 확인됨 → YOLO 업로드 기다릴 필요 없이 바로 진행 가능
- 현재 `production_plan` 4건뿐 — 필요시 추가 등록 여부 결정 필요
- `sensor_data`/`alarm_threshold`/`alarm`/`agv` 시딩은 우선순위 낮음, 핵심 체인(계획→배치→검사→검토) 완료 후 진행

#### 2. YOLO fixture + Release 업로드

```
docker-compose.yml에 이미 배선되어 있음:
  YOLO_MODEL_PATH: /app/backend/models/best.pt
  volumes: ./backend/models:/app/backend/models:ro
→ 서버 backend/models/ 폴더에 실제 best.pt 파일만 배치하면 됨
```

- `best.pt` 모델 가중치 파일 소재 미확인 — 누구 컴퓨터/드라이브에 있는지부터 확인 필요
- CI의 `backend-test-yolo` 잡이 참조하는 fixture 데이터도 별도 확인 필요
- 파일 확보 후: GitHub Release 첨부 또는 scp 직접 전송으로 서버 배치 → 추론 테스트

#### 3. 그 외 정리 항목 (급하지 않음, 여유 시)

- Default Branch를 `dev → main`으로 되돌리기 (소유자 계정 로그인 시 처리)
- CI에 MariaDB 서비스 컨테이너 추가 (8/18 예정)
- `.env.example`의 `DB_USER=root` 기본값 수정

⚠️ **주의**: SSH 키(`pcb-mes-prod-deploy-v2`)는 실습실 PC에만 있고 다른 컴퓨터로 옮겨진 적 없음. 다른 환경에서 작업 시 새 키를 발급해 서버 `authorized_keys`에 추가하는 방식으로 진행할 것 (기존 키 복사 시도는 형식 오류로 여러 번 실패했음).