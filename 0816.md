# CI/CD · AWS 배포 작업일지 — 8/16(일)

> **저장소** KDTbigdata8th/pcb-defects-mes · **발표** 2026-08-22 (토) · **작성일** 2026-08-16
> 전체 누적 기록은 `CICD_AWS_작업일지_요약.md` 참조 (해당 문서의 `### 8/16` 절에도 동일 내용 병합됨)

---

## 오늘 목표 대비 결과

| 계획(어제 To-Do) | 결과 |
|---|---|
| 데이터 시딩 실행 (배치/검사) | ✅ 완료 |
| YOLO fixture + `best.pt` 모델 업로드 | ⬜ 미착수 — **8/17로 재이월** |
| (계획 외 신규 발견) 공정상태 화면 다중 사용자 동기화 버그 | ✅ 발견부터 수정·배포까지 완료 |

---

## 1. 데이터 시딩 실행

기존 `production_plan` 4건(plan_id 1~4) 전량에 대해 배치 생성 → 라인 이송 완료 → 검사(시뮬레이션) 순서로 진행.

**API 흐름**
```
POST /api/plans/{plan_id}/batch
POST /api/batches/{batch_id}/line-transfer/complete   (body: transfer_id)
POST /api/batches/{batch_id}/inspect
```

**결과**

| plan_id | batch_size | inspection_count | review_count |
|---|---|---|---|
| 1 | 8 | 8 | 0 |
| 2 | 8 | 8 | 0 |
| 3 | 460 | 460 | 31 |
| 4 | 100 | 100 | 8 |

**합계**: `production_batch` 4건, `batched_pcb`/`inspection_result` 각 576건, `defect_review` 39건 정상 생성. DB 카운트로 최종 검증 완료.

**진행 중 겪은 문제**
- `curl.exe`가 이 컴퓨터에서 원인 불명으로 실행 자체가 차단됨(`Access is denied.`, "현재 PC에서는 이 앱을 실행할 수 없습니다" 팝업). Windows 보안 보호 기록에도 특별한 차단 이력 없음. **PowerShell `Invoke-RestMethod`로 전환해 우회**, 근본 원인은 미해결로 남김.
- curl이 막힌 동안 보낸 요청 일부가 실제로는 서버에 정상 처리되어 있었음(`PLAN_ALREADY_BATCHED` 에러로 뒤늦게 발견) — 클라이언트가 응답을 못 받은 것과 서버 처리 실패는 다르다는 점을 재확인, DB 직접 조회로 실제 상태 파악 후 이어서 진행.
- plan 4 배치 생성 시 `LINE_MODEL_CONFLICT`(plan 3이 라인 점유 중) → plan 3을 후검사 버퍼로 이송하려 하니 `POST_INSPECTION_BUFFER_FULL` 발생 → 아래 2번 문제로 이어짐.

---

## 2. `post_inspection_buffer_slot` 0건 문제 — 발견 및 해결

**증상**: 슬롯이 분명 16개 있어야 하는데(migration 정의상) 실제 조회하면 0건, 그래서 버퍼 이송 자체가 항상 거부됨.

**원인**: 8/6 migration(`backend/db/migrations/20260806_add_process_status_lifecycle.sql`)에 16개 슬롯 초기 INSERT가 정의되어 있으나, 이 파일은 `docker-entrypoint-initdb.d`에 마운트되어 있지 않음. `docker-compose.yml`엔 `schema.sql`(테이블 구조)만 마운트되어 있고, 이 초기화 스크립트는 **DB 볼륨이 처음 생성될 때 딱 한 번만 실행**되는 구조. 우리 서버는 볼륨을 계속 재사용해왔기 때문에, 이 migration이 서버 DB에는 한 번도 반영된 적이 없었음.

**조치**: 16개 슬롯 수동 INSERT (`slot_no` 1~16, `status='EMPTY'`)로 즉시 해결.

**부가 발견**: `POST_INSPECTION_BUFFER_FULL` 에러 메시지가 "테이블 행 자체가 0건"과 "행은 있으나 EMPTY 상태가 0건"을 구분하지 않고 동일하게 반환함 (`process_status/repository.py`의 `WHERE status='EMPTY' LIMIT 1` 조회가 `None`이면 무조건 같은 에러). 지금 당장 문제는 아니나 향후 디버깅 시 혼동 여지 있어 기록.

**남은 일**: 이 "migration 자동 미반영" 구조적 문제 자체는 아직 정식 해결 안 됨 — 8/18 이후 배포 스크립트 개선 또는 `docker-entrypoint-initdb.d` 마운트 방식 변경 검토 필요.

---

## 3. 공정상태 화면 로직 검증 (3건)

사용자가 데이터 시딩 중 화면 동작에 의구심을 제기하여, Codex를 통해 순차적으로 코드 근거를 확인.

### ① 트랜잭션 범위 — 문제 없음
- 계획/배치 생성: 배치 전체가 원자적 트랜잭션
- `/inspect`(시뮬레이션): 배치 전체가 원자적 트랜잭션
- YOLO `/start`(비동기): **PCB 단위 개별 트랜잭션** — 장시간 비동기 검사를 고려한 의도된 설계. 중간 실패 시 이미 처리된 PCB 결과는 남고 배치가 `INSPECTING`/실패 상태로 남을 수 있음 (8/12에 만들어둔 STALE 배치 처리가 이런 상황의 안전장치로 추정 — 8/17 YOLO 실측 전 재확인 권장)
- review 확인, 공정 이송(버퍼/최종): 호출별 트랜잭션
- 결론: 전체 업무 흐름을 하나로 묶은 트랜잭션은 없지만, 이는 락 장시간 점유를 피하기 위한 표준적인 설계 패턴으로 판단 — 수정 불필요

### ② batch 3/4가 `/inspect` 이후 다르게 동작(버퍼 자동 이송 여부)한 원인 — 버그 아님
- 백엔드 `/inspect`는 검사만 수행하고 버퍼 이송을 자동으로 트리거하지 않음 (코드로 확인)
- 실제 원인은 **프론트엔드**(`ProcessStatus.jsx`)에 있음 — WebSocket으로 상태 변화를 감지해서, `status=INSPECTED && logistics_location=LINE` 조건을 만족하면 화면이 자동으로 `post-inspection-transfer/start`를 호출하는 구조
- batch 4는 화면이 열려있는 상태에서 검사가 끝나 자동 진행됐고, batch 3은 당시 버퍼 슬롯이 0건이라 자동 시도가 있었더라도 실패했던 것으로 추정
- 결론: 의도된 기능. 데모 중 "아무도 안 눌렀는데 배치가 저절로 이동"하는 걸 보더라도 정상 — **팀 공유 필요** (아래 5번)

### ③ 새로고침 시 AGV 애니메이션이 경로 시작점부터 재생되는 문제 — **실제 버그로 확정**
- DB 상태 자체는 안전함 재확인: `post-inspection-transfer`, `final-dock-transfer`의 start/complete 모두 이미 처리된 상태에서 재요청 시 `idempotent=true`로 안전 처리 (관련 테스트 존재: `test_lifecycle_service.py`)
- 다만 `resumeServerTransfer()`(`processStatusModel.js`)가 `batch.transfer_started_at`을 전혀 사용하지 않고, 새 브라우저 세션·새로고침 시 무조건 `agv.position = 시작점`, `routeIndex = 0`으로 초기화
- **애니메이션 이동 시간도 서버의 실제 경과 시간과 무관** — `이동속도(18) × 경로길이`로만 계산되고 `transfer_started_at`과 연동 안 됨
- **심각도 판단**: 이 프로젝트를 AWS에 배포한 목적 자체가 여러 사용자가 동시에 접속해서 보는 시연 지원인데, 서로 다른 시점에 접속한 두 브라우저가 같은 배치를 보면서도 서로 다른 AGV 위치를 표시하게 됨 — **YOLO 모델 업로드보다 우선순위를 올려 즉시 수정 착수 결정**

---

## 4. AGV 애니메이션 동기화 버그 수정 (완료·배포)

**브랜치**: `fix/agv-animation-sync` (dev에서 신규 생성)

**변경 파일**: `frontend/src/domains/agv/agvSimulation.js`, `frontend/test/features/process-status/processStatusLifecycle.test.js`, `.gitignore`

**핵심 로직**
- `positionAlongRoute(route, distance)` 신규 함수 — route 배열을 세그먼트 단위로 순회하며, 주어진 거리만큼 이동한 지점의 정확한 좌표와 `routeIndex`를 계산 (선형 보간)
- `resumeServerTransfer()` 수정 — `Date.parse(batch.transfer_started_at)`으로 경과 시간 계산 → `경과 초 × MOVE_SPEED(18)` = 이동 거리 → `positionAlongRoute()`로 실제 위치 계산 → 그 위치에서 애니메이션 시작
- 이동 시간이 route 전체 길이를 초과하면(이미 도착했어야 하는 경우) 경로 마지막 지점에 배치, 기존 도착 처리 흐름과 자연스럽게 연결
- `transfer_started_at`이 없는 경우 기존처럼 0%에서 시작하는 fallback 유지
- 같은 React 세션 내 기존 AGV 이어받기 로직(`findInFlightTransferAgv`)은 미변경 — 이번 수정은 새 세션에서만 작동
- 파일 최상단에 모듈 전체 동작 원리 요약 주석 추가 (한글 — 기존 파일 전체는 영어 주석이라 톤이 일부 불일치, 발표 후 정리 검토 대상으로만 기록)

**검증**
- 신규 테스트 3건: 경과 시간 기반 중간 위치 복원, 이동 시간 초과 시 도착 지점 처리, `transfer_started_at` 없을 때 기존 동작 유지
- 전체 프론트 테스트 522 passed, `npm run build` 성공

**한계**: 위치 계산이 클라이언트의 `Date.now()` 기준이라, 접속 기기의 시스템 시계가 크게 틀어져 있으면 이론상 오차가 발생할 수 있음. 대부분의 기기는 NTP 자동 동기화로 이 문제에 해당하지 않아 실질적 리스크는 낮다고 판단.

**배포 이력**
- `fix/agv-animation-sync` → dev (PR #46) → main → `Run workflow`(main, Deploy #7, 25초) → `Success` 확인

**작업 중 발견한 별개 이슈**: `.gitignore`의 SSH 키 무시 패턴이 `pcb-mes-prod-deploy*`로 고정되어 있어, 이 노트북에서 새로 발급한 `pcb-mes-notebook-deploy` 키가 무시 대상에서 빠져 커밋될 뻔함(untracked 상태로 노출). `pcb-mes-*-deploy*`로 패턴을 넓혀 조치. 프로젝트 폴더에 원인 불명의 0바이트 파일들(`curl.exe`, `git`, `how efcb842 --stat`)도 함께 발견되어 삭제.

---

## 5. 팀 공유 필요 사항 (정리)

```
[공정상태 화면 관련 공유 — 8/16 확인된 3가지]

1) DB 볼륨 재사용 시 migration이 자동 실행 안 됨
   → 신규 migration은 서버에 수동 반영 필요, 자동화 방법 논의 필요 (8/18 예정)

2) 공정상태 화면이 검사 완료 배치를 자동으로 다음 단계까지 진행시킴
   → 프론트가 상태 감지해서 자동 호출하는 의도된 기능, 데모 중 놀라지 않아도 됨

3) [8/16 수정 완료] 새로고침 시 AGV 애니메이션이 처음부터 재생되던 문제
   → transfer_started_at 기반으로 실제 경과 위치에서 시작하도록 수정, main 배포 완료
   → 여러 사용자가 동시 접속해도 동일한 AGV 위치를 보게 됨
```

---

## 내일(8/17) 최우선 순위

1. **YOLO 모델(`best.pt`) 소재 확인 + 서버 업로드** — 8/15, 8/16 두 번 밀린 항목, 반드시 착수
2. YOLO 배치 실측 (4GB 한계 확인, 8GB 증설 여부 판단)
3. 8GB 증설 시 Ollama 병행 가능성 측정 (챗봇 관련, 여유 있으면)
4. YOLO 비동기 검사(`/api/inspection/batches/{id}/start`)의 PCB 단위 트랜잭션 구조 특성상, 대용량 배치(예: 460개짜리) 실측 중 중간 실패 시나리오도 함께 확인 권장 (STALE 배치 처리 로직 실증)

---

## 참고 — 오늘 최종 수치

| 항목 | 값 |
|---|---|
| production_batch | 4건 |
| batched_pcb / inspection_result | 576건 |
| defect_review | 39건 |
| post_inspection_buffer_slot(EMPTY) | 시딩 종료 시점 14건 (2건 OCCUPIED: batch 3, 4) |
| Frontend 테스트 | 522 passed |
| 배포(Deploy) 성공 횟수 | 1회 (Deploy #7, main, 25초) |





테스트 중...

{"batch_id":2,"batch_code":"BATCH-20260813-0002","plan_id":2,"created_at":"2026-08-15T23:26:29","batch_size":8,"target_defect_rate":0.00198,"true_defect_count":0,"random_seed":5784604225078295611,"line_id":5,"normal_PCB_count":null,"status":"LINE_ASSIGNED","smd_handoff_at":null,"logistics_location":"TO_LINE","logistics_updated_at":"2026-08-15T23:26:29","current_transfer_id":"21513ad36bce4e558aa96cfd2a084eed","transfer_started_at":"2026-08-15T23:26:29","final_quality_verdict":null,"verdict_locked_at":null}


C:\Users\kjy92\Desktop\KDT2026\KDTTeamProject>curl.exe -sS -X POST "http://3.38.222.241/api/batches/{batch_id}/line-transfer/complete" -H "Content-Type: application/json" -d "{\"transfer_id\":\"21513ad36bce4e558aa96cfd2a084eed\"}"